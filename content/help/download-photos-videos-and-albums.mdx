---
title: Download your photos, videos, and albums
description: "Every download is the original file at full quality. Save one from the photo viewer, or Download album for a zip of everything, just photos, or just videos. Guests can do both. HEIC files stay HEIC."
category: sharing-and-downloads
order: 1
updated: "2026-09-01"
audience: both
keywords:
  - download
  - download all
  - zip
  - original quality
  - export
  - HEIC
  - Windows
  - print photos
---

Nothing you download from Partyreel is a copy of a copy. Every file is the original that was uploaded, byte for byte, with the location metadata already stripped (for JPEG, PNG, WebP, MP4, and MOV; HEIC and a few others are kept as they arrived).

## One photo or video

Open it in the photo viewer and tap <UiLabel>Save</UiLabel>. On a desktop, the same <UiLabel>Save</UiLabel> appears when you hover a tile. The file is named after the event with a short id, and it's the full-size original. Videos download as the uploaded file, not a re-encode.

## The whole album

<Path>Dashboard › Your event › Gallery › Download</Path>

Tap <UiLabel>Download</UiLabel> in the gallery header (guests have <UiLabel>Download all</UiLabel> just above the album). The <UiLabel>Download album</UiLabel> dialog offers three bundles with live counts, <UiLabel>Everything</UiLabel>, <UiLabel>Photos</UiLabel>, and <UiLabel>Videos</UiLabel>, and shows the total size. Hosts also get <UiLabel>Include hidden items</UiLabel>, which appears only when there's something hidden or waiting. Tap <UiLabel>Download</UiLabel> and the zip streams straight to your device.

A very large album may show <UiLabel>Too large to download all at once. Pick photos or videos to split it up.</UiLabel> Split by type, or use Select mode to download in batches: pick tiles, then <UiLabel>Download</UiLabel> in the bar (that bundle skips the dialog and includes hidden items). See [select many photos at once](/help/bulk-select-and-batch-actions).

## What guests can download

Exactly what they can see. A guest with full access can save any single photo and bundle the whole album; hidden and unapproved items are never in their zip. A visitor who only sees the preview can download only that preview. There's no host switch to disable downloads: if someone can open the album, they can keep it, which is what the album is for. The guest side is in [browse the album](/help/browse-the-album).

## HEIC files on Windows

iPhones shoot HEIC, and Partyreel keeps the file as it came, so a downloaded album can contain `.heic` photos. They open natively on a Mac, an iPhone, or an iPad. On Windows, install the HEIF Image Extensions from the Microsoft Store, or convert the files with any photo app that exports JPEG. Thumbnails show everywhere; the full-size view of a HEIC opens only in Safari, so on Windows or Android save the file and open it in a photo app.

## Printing your photos

Download the original and take it to any print service or photo app; the files are full resolution. Partyreel doesn't offer prints.

<Callout type="tip" title="Download before you delete">

Deleting an event removes its media for everyone, and nothing in Deleted can be downloaded. Grab the <UiLabel>Everything</UiLabel> bundle first.

</Callout>
