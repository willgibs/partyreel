# Partyreel — Status (you-are-here)

> ROLE: the live snapshot: the era, the current round and the one before, live state, infrastructure,
> what waits on Will. BELONGS HERE: what is true right now. · NOT HERE: the rules (→ [`PROGRAM.md`](PROGRAM.md)),
> what shipped (→ [`CHANGELOG.md`](CHANGELOG.md), two rounds deep), how systems work (→ [`systems/`](systems)),
> what might be next (→ [`ROADMAP.md`](ROADMAP.md)). GROWS BY: being replaced at every round close.

**Updated:** 2026-09-20

## The era

Partyreel is in pre-launch continuous elevation: the full product is built and live at partyreel.com with
zero real users, Stripe in TEST mode, and the launch switches deliberately unspent (they accrete in
[`ROADMAP.md`](ROADMAP.md) → Launch checkpoint). The elevation program ([`PROGRAM.md`](PROGRAM.md)) is the
only active thread: work rides `launch-prep` in rounds, each a catalog in the lab, Will's verdicts on the
desk, then the wiring; partyreel.com changes only at tagged milestone merges. The goal for this stretch
(Will, 2026-09-14): every page reaches a cohesive informational flow and every point of the design system,
the marketing site and the app is elevated platform-wide; nothing is protected, and since 2026-09-19 the host app and
the guest pages are explicitly open to be reconceived from the ground up ("closer to a Frankenstein's monster";
his words in `design/rulings.md`).

## The current round: the closing sitting (opened 2026-09-20 at the first batch's record; the small hours before it)

The closing sitting: no new board until the desk is closed (his rule of 12:20 EDT); his batch of 18:20 EDT answered the
desk's first five boards (14 verdicts; the plan approved in plan mode after a red-team of the identity brief). Four
wiring lanes retire their boards, the desk pass badges the reach, and `guest-verify` round two draws the identity
shape whole while his four rulings there are held on his "May have to relitigate". Moltbook is paused on his word.
His steer binds: fast iterative rounds; nothing is protected; everything on identity is open to relitigation.

| track | returns | state |
| --- | --- | --- |
| `heroes` | `privacy-hero` (the field as two spirals, faster, a trail) and `album-page` (round four at the home hero's pace, subtle, the live album beneath at 896 with its bottom faded, a pool of light) | `album-page` wired and retired at `2ca47448` (`album-wiring`: the live album under the host's header, the halo, the floor with a photograph section beneath); `album-motion` on the desk with three variations of the fall on the wired hero; `privacy-hero` round three on the desk at `6c99e128` (a breathing aperture, a grid where tiles take turns clearing, sealed cards that lift) |
| `voice` | the eight lines derived from won lines | wired and retired at `32861973` (`voice-wiring`, 2026-09-20: "No app required." on 27 sites under one written rule, his hero sentence with the meta line split off, `PRO_LINE`, the album noun on both empty states, his gate line); on the alias for Will's eye, eight calls his in the CHANGELOG |
| `glass` | round one on the app's chrome over photographs; round two on the ONE material | wired and retired at `a2a0d973` (`glass-wiring`, 2026-09-20: Crystal with the double edge as tokens and one utility, the lightbox on a half-bright blurred album, one MediaTile for every grid on explicit columns, the reel's pane, the chip on paper); three calls his in the CHANGELOG; the host's row, the bin and the chip await his signed-in eye |
| `admin` | the admin portal's shape, round one | wired and retired at `b81ed49a` (`admin-wiring`, 2026-09-20: four figures with the ranked queue beneath, the 44 px bar, the 232 px rail, the health band, the ⌘K palette on a new primitive, tables with tone, four state colours on the row, one destructive sheet on nine controls); its fixtures a Library demo; the portal is Will's signed-in eye; three calls his in the CHANGELOG |
| `loose-ends` | six ROADMAP lines as seven decisions on their real surfaces: the admin chart cast in both modes, one FAQ look, the hero at a 900 px tablet, the album's three ambient pieces | integrated at `b83b7c3d`; on the desk; one question his (the reading of the two ambient pieces that had nothing to vary); a real chart-token bug found, on the ROADMAP's Now list |
| `body-type` | the body and label ladder; the button rung | RETIRED at `224049d6` (`buttons-wiring`, 2026-09-20): the button rung wired (the icon one notch over its text; `sm` on the ladder; the door's five overrides onto `cta`); six rungs at `59345bc8`, the body scan closed at `f76ff412`; `icon-sm` at 14 his to overrule |
| `app-shape` | the host app's shape; the home across host states | round one wired (`home-wiring` `62a82a26`, `hub-wiring` `a91464cb`; the signed-in surfaces his on the alias); round two RULED 2026-09-20 18:20 EDT (`empty=wizard`, `first=pulse`, `busy=collapsed`; his note: Just arrived beneath the events, notices and storage above); `home-states-wiring` cut at the record; the board retires at its merge |
| `guest-shape` | the guest experience's shape; the chrome, the welcome's shell, theirs | round one wired at `7f4f2ffe` (`guest-wiring`); round two RULED 2026-09-20 18:20 EDT (`chrome=both`, `welcome=sheet`, `theirs=mark`; his note: the Yours filter and tile size in one parent menu); `guest-chrome-wiring` cut at the record; the board retires at its merge |
| `app-vocabulary` | the parts under both shapes; where the host gallery's controls live | RETIRED at `ddf4db47` (`controls-home-wiring`, 2026-09-20): one View menu (tile size, Sort disabled until the list is whole, Filter with the Deleted lens) beside Add photos, Download and Select; round one at `40863a96` (`vocab-wiring`); the guest album mounts the same menu under `guest-chrome-wiring` |
| `contact-page` | how someone reaches a person at Partyreel: whether a form is required, the receipt, urgency, the topic, the page against the cinema rhythm, what stands beside the form; six decisions on the real desk | integrated at `a8afce0c`; on the desk; three findings on the ROADMAP |
| `press-page` | what Partyreel hands the world about itself: who the page is for, the sheet, the words, the facts, a human, the close, the arc; seven decisions on the real page pieces | integrated at `060dfdf4`; on the desk; one question his ("Live now" pre-launch; recommended: leave it) |
| `app-pricing` | pricing inside the app: what a click opens, what it opens on, how much it holds, how the marketing page stays a click away, the pass, the doors, how a locked control asks, what Checkout returns to; eight decisions on the shipped chrome with four hosts | integrated at `0379c529`; on the desk; three questions carried on their recommendations (in the CHANGELOG); its `doors` third option is app-shape's `you`, drawn |
| `pricing-page` | the marketing pricing page; round two on Find your plan size and the phone row | round two on the desk at `3cf43bde` (2026-09-20: `split` and `swipe` recommended, the phone demo repaired); six of eight wired at `58f7acbd` (`pricing-wiring`, 2026-09-20: the paper opening under the cinema bar, the pair with a range slider, the Pass a ticket, one dark room from the tiles to the table, six FAQ items); the phone row held until its demo works; four calls his in the CHANGELOG; round two on fit and the phone row queued |
| `app-door` | the door into the host app; round two on the welcome tour | round two on the desk at `af7ec784` (2026-09-20: `film` recommended); the door wired at `f7075a73` (`door-wiring`, 2026-09-20: one AccountDoor worn four ways, the code first, the product beside /login, an existing account named after a verified code, failures with buttons, passkeys dark behind a flag until Will enables them in the Supabase dashboard); two calls his in the CHANGELOG; round two on the tour queued |
| `first-event` | a host's first event from "Create" to a code on the table: what creating asks, where the style is chosen, the Free limit, how the code reaches the venue, where the host lands, what a host holds out at the door, the empty event, the first photograph; eight decisions on the real create card and real QR plates, every code's module edge measured | integrated at `728513ee`; on the desk; six questions his (in the CHANGELOG); two product misses on the ROADMAP (the swatches under the scan floor; the style step's 404 link) |
| `guest-upload` | the moment a guest adds a photograph: the tap, sending, held, failed, the batch, the landing, the warning, the words; eight decisions on the shipped guest components over one wedding, phone first | integrated at `1649506c`; on the desk; four questions his (in the CHANGELOG); the batch step's measured numbers are its own argument |
| `demo-event` | the demo as the product's first impression; round two on the doors | round two on the desk at `042f82bd` (2026-09-20: the door as one object at four places; `frame` recommended; the wired pile reached only the nav panel, the winner's wiring covers the hero and the feature line too); answered whole (2026-09-20: a role on arrival, the Demo mark that stays, the turn after an upload, Start your own in the row and a closing card, the photo pile as the door, one session across two screens, one party); wired whole at `0521613a` (2026-09-20; the pairing proven with a listener, a real phone Will's); round one |
| `media-viewer` | what a photograph opens as when a guest or a host taps a tile: the opening, what it holds, who, next, close up, video, the way out, a link; phone first | integrated at `596ca8dd`; on the desk; three calls carried on their recommendations (in the CHANGELOG); one asset asked (ASSETS row 23); three ROADMAP Now lines |
| `reel-studio` | the highlight reel from the studio to a guest's hands: the door, the room, the styles, the moments, a blocked tile, sharing, the wait, how a guest watches; eight decisions on local replicas, never the portal | integrated at `d10149bd`; on the desk; four calls carried on their recommendations (in the CHANGELOG); five ROADMAP Now lines (the dead rail, the cap copied four times, the poster card's width, a stale doc line, the cover strip's shape) |
| `host-curation` | the host's act of reviewing what guests send: the queue, the verb, the peek, keys, undo, new arrivals, the count, whether a refused guest is told; eight decisions on the shipped review surface | integrated at `ff09a50f`; on the desk; five calls carried on their recommendations (in the CHANGELOG); a shipped bug on the ROADMAP's Now list (the hidden-media dim has never rendered) |
| `admin-triage` | the operator's act on a report inside the admin board's shape: what a report looks like, a wordless one, what a verdict costs, what a closed one leaves, the legal-hold door, the phone, one idiom for four inboxes, who is told | integrated at `30db05e5`; on the desk; four calls carried on their recommendations (in the CHANGELOG); five ROADMAP lines |
| `emails` | every email Partyreel sends, the real templates in an inbox mock: one shell, the brand, the sender, the foot, the code, the moments, the guest's, the dark inbox | integrated at `2135ce6e`; on the desk; no questions; three ROADMAP Now lines (no plain-text twin, `sendOnce` untested, a stale PRICING line) |
| `help-center` | where a host or a guest with a problem lands: who first, the hub, the article, from the product, feedback, the dead end, search; seven decisions on the real help pieces | integrated at `5118c141`; on the desk; four calls carried on their recommendations (in the CHANGELOG) |
| `profile-page` | what a person is on Partyreel beyond one album; round two on View all, the quick-look and the way back | round one wired at `062498c3` (`profile-wiring`, 2026-09-19: the page on the album's header with a guest-bar 404, one grid of cards marked Host or Guest, a capped bio, Report this person over Block with an operator queue, everyone named, the handle free and offered after an upload, a faces row above twelve; the migrations applied, a corrective third restoring the RPC's anonymous-viewer gate); on the alias for Will's eye, nine calls his to overrule (CHANGELOG); round two on the desk at `73cde345` (`profile-reach`: `view-all`, `quick-look`, `way-back`) |
| `how-it-works`, `error-pages` | the page that tells the loop; every failure page as one grammar | wired and retired at `ef948322` (`loop-wiring`: one scroll, the Host/Guest toggle over six steps a side, twelve bespoke pictures, the demo as the payoff, the stepper overview on the home, the footer heading a rung down) and at `98909b34` (`errors-wiring`: `NotFoundScreen` the one primitive with visual, help and digest slots, a root error boundary, the strip as the marketing icon, the portal 404 on the admin host; Sentry one event per crash, none per 404); both on the alias for Will's eye, five calls each his to overrule (in the CHANGELOG) |
| `export-flow` | getting everything out, for a host and for a guest: what download means, the wait, stuck, hollow, the cap, the object, the teaser's chips, the phone; on the real dialog's shell and body over fixture summaries | integrated at `afde9ca3`; on the desk; five calls carried on their recommendations (in the CHANGELOG); seven ROADMAP lines |
| `site-chrome` | the marketing chrome; round two on the footer against the closing CTA | round one wired at `6c76a08c` (`chrome-wiring`, 2026-09-19: the bar hides going down and returns coming up, a Dashboard hint from the cookie prefix, Start free above the demo check at every width, both nav doors to `/how-it-works`); on the alias for Will's eye, seven calls his to overrule (CHANGELOG); round two on the desk at `2aa1dc5e` (`footer-close`: `foot-after`, `foot-alone`, `foot-phone`) |
| `event-identity`, `event-type-pages` | the event pages' identity and the direct picks | wired and retired at `6abab6a1` (`events-wiring`, 2026-09-19: one lit object per type carrying the demo's real code, the statement, the photograph turn into one paper chapter, the photograph as the card at two sizes, the river door with the reel beside it, the hero subhead on a 20 to 22 clamp site-wide, the phone half-and-half; conferences and trips on named stand-ins until ASSETS rows 24 and 25 land); on the alias for Will's eye, nine calls his to overrule (CHANGELOG) |
| `overtaken` | the desk mechanism his ruling asks for: a question an earlier ruling reaches stays badged in its walk with one agent line and "The ruling stands" beside "Not clear to me"; an answer overrides and is echoed to the overtaking board | integrated at `f7928597` (2026-09-20); the second pass at `ca9bc121` for the sixth batch: 76 asks badged (47 new; 68 stand, 8 concede), 66 still open; the `stands` grammar at `7f0ca050` |
| `guest-verify` | confirm the email, or badge it; round two on the identity shape whole | round two on the desk at `d68ef23f` (2026-09-20): five decisions, `none`, `handful`, `shown-marked`, `offer`, `one` recommended (a third shape; no new identity column); round one's four rulings HELD on the board; the wall corrected to Supabase's current table; needs his three configured Auth rate limits (`call:numbers`) |
| `toasts` | the toast as a system: a new board from his `moment=today` note | RULED whole 2026-09-20 20:05 EDT (`top`, `card`, `persist`, `expanded`, `always`: every one the recommendation, no notes); `toasts-wiring` cut at the record; the board retires at its merge |
| `help-sync` | the help articles track shipped reality again | merged `84ee48cd` (2026-09-20): eleven articles against the tree, the two legal lines, the legal versions bumped (privacy 1.2, terms 1.3) |
| the closing sitting's lanes | `buttons-wiring`, `controls-home-wiring`, `home-states-wiring` (Sonnet), `guest-chrome-wiring`, `guest-verify` round two, `overtaken-3` (Opus) | cut 2026-09-20 at the record on :3131 to :3136; the sixth batch's lanes all merged by 13:40 EDT; the signed-in surfaces no lane can reach (the hub gallery's View menu, the dashboard's order and collapse, the admin portal, the account page's passkey row) are Will's on the alias |
| `lab-tides` | the lab itself: ten workflow findings (the demo's guards and budgets, the constructor's dedupe and `today`, the frame's doctype, the responsive-variant cause measured, the carried calls, the sent marker, the idempotent re-send) | integrated at `75d6d2e2` (2026-09-20); the `call:` grammar landed at `e9e3d25e`; the UNPAINTED cause found by `glass-material` (a clipped capture, not the renderer; round two's steps paint) |
| `seed-avatar` | the colour a new account is; round two on the look | round two on the desk (2026-09-20: `mesh` recommended, hashvatar's register from its source); wired at `2a5c7018` (`avatar-wiring`, 2026-09-20: the disc clipped, the generator home in `lib/avatar` with a hashed account seed, the diagonal under the initial and the photograph on every surface, a fourth size for the profile row); four calls his in the CHANGELOG; round two on richer looks queued |

## The previous round: the night sitting (2026-09-20, `69a9a177` to `806695d1`)

The fifth batch's six lanes wired the voice's eight lines, the body ladder, the pulse and the events list, the event hub
with its sheets and the overtaken mechanism, and glass round two measured the one material; every lane on the alias the
same night. Its record is the CHANGELOG's second entry.

## Live state

- **Prod (partyreel.com)** = `main` @ tag `milestone-26` (`df173c2e`, merged 2026-09-18 late from the `launch-prep` tip
  `353ad884`); `launch-prep` is ahead by the overnight round's records. **The admin cutover is complete** (2026-09-18, late):
  `admin.partyreel.com` is served by `partyreel-admin` (`NEXT_PUBLIC_SURFACE=admin`, the allow-list) and the apex by
  `partyreel` (`=app`, so `/admin` is a 404 there); the cron runs on the app surface only; every runbook check is done.
  **The `launch-prep` alias** (`https://partyreel-git-launch-prep-partyreel.vercel.app`) serves the fifth
  batch's wave and glass round two (`806695d1`, 2026-09-20; the sixth batch's record moves it next); Will reviews on it as well as his local `pnpm dev`, so **the alias is rebuilt whenever a board changes**. Vercel's cap is
  100 deployment creations per trailing day (canceled ones included): on 2026-09-19 it held the alias at `89548cbb` for a
  day, so lane branches no longer create deployments (`vercel.json`), `[preview]` stays the Orchestrator's and the prune runs after every integration. The wiring lanes' surfaces were
  red-teamed on it on 2026-09-19 signed out and, in Will's Chrome session, signed in (the album page, the QR door, `/demo`, the
  guest album at 1280 / 1512 / 1920 / 375 both ways, the host event page at 1440 and 375, the dashboard, the root 404): clean;
  batch three's surfaces the same evening (`/how-it-works` both sides and the home's stepper at 1440 and 375, the mega panel's card, the help hub's link, the footer heading; the root, paper, cinema, guest and host 404s, the private lock, the host crash with its copyable code, the admin host's `/nope` and `/features` on its own launch-prep alias; Sentry one `render:app` per crash and none per 404): clean.
  Batch four's wiring late the same night on `b04ecd54`, signed in at 1440 in Will's Chrome and signed out at 375 and 973 in the pane: the Dashboard hint in the header (one pill in the pair's place), the bar leaving on a real scroll and returning on the way up at every width and posture (`data-hidden` measured), the Resources card and the Features line both on `/how-it-works`, Start free in the footer at 375 with the demo beside it; `/u/willg` on the album's header with the account menu, the Host card, a bio saved from `/account` and rendered under the name level with the avatar (then cleared), `/u/nope` on the guest bar; the events hub's object and 2x2 photograph directory, `/events/weddings` end to end (the code a real `/demo` door, the statement, the turn, the paper chapter, the river door with the reel beside it), the object crossing the fold at 375; the three copy fixes. NOT exercised, and why: the faces row past twelve and the Guest-marked card (no real data), a person report through the menu and `/admin/reports` (no second handle, no admin session), the after-upload claim line (a Free account without a handle is a password sign-in), the empty bio save (the extension could not clear the field; the row was cleared by SQL).
- **Data:** disposable test data only (3 profiles / 3 events / about 16 media rows); the accounts and fixtures are in [`systems/testing-verification.md`](systems/testing-verification.md).
- **Tests:** about 2,805 green (`pnpm test`); the gate is typecheck + lint + test + build, run locally (CI runs on `main` and
  `launch-prep` code pushes only; an `lp/*` push only on `[ci]`); `lab:smoke` passes whole, so a board over its reading budget is a failure.
- **Jobs:** the daily purge cron, the media-backup Worker and the daily DB-backup Action (about 06:30 UTC) are live; the
  deletion-aware backup prune ships in dry-run (`PRUNE_MODE=live` is a launch flip); the Worker's queue and dead-letter depth reading is deployed (`d7b16bcc`).
- **The repo is public for the interim** (Will, 2026-09-15, after twelve tracks spent the private GitHub Actions minutes in two days); private again when the budget clears.

## Infrastructure

All backing services run under the owner account **partyr33l@gmail.com ("P3")**: **Supabase** `ddafaemglzmuekbtjwzn` (Pro,
daily backups, the public `avatars` bucket); **Cloudflare R2** `8bd90d2f6a374d6cdff2f379e929b060`, buckets `partyreel` and
`partyreel-backup`; **Stripe** `acct_1TcStrPtjqmVkBwk` (TEST; the live cutover is a launch task); **Sentry** org `partyreel`;
**Resend** (`partyreel.com` verified; auth email rides Resend SMTP); **Google OAuth** P3 web client; the in-app operator
`partyr33l@gmail.com` (`is_admin` + TOTP MFA); **Vercel** on the P3 team (two projects on one repo, `partyreel` and
`partyreel-admin`; Hobby; the Pro cutover, DNS to Cloudflare and the repo transfer are launch-time cutovers). "Allow new
signups" stays ON (anonymous sign-ins OFF). Already configured, never redo: the R2 buckets, creds, CORS and the
abort-multipart rule; the apex domain; `CRON_SECRET`; `profiles.is_admin`; the Stripe TEST products, prices, webhook,
Billing Portal and its ten env values; Supabase TOTP MFA with the admin callback in the redirect allow-list (break-glass:
delete the factor in `auth.mfa_factors`); the Sentry project and its env; the backup Worker and Action secrets; the prune
crons and `PRUNE_API_SECRET`.

## Waiting on Will

His sitting continues on the desk: his batch of 18:20 EDT answered the desk's head (five boards); twenty-three boards with open asks remain in the desk's own order, which is by leverage since 2026-09-19 (the earlier influence first; his rule, verbatim in `design/rulings.md`): `app-pricing`, `first-event`, `guest-upload`, `media-viewer`, `host-curation`, `reel-studio`, `export-flow`, `admin-triage`, `help-center`, `emails`, `site-chrome` round two (the footer), `profile-page` round two (View all, the quick-look, the way back), privacy-hero round three, `album-motion`, `loose-ends`, `contact-page`, `press-page`. Plus `pricing-page`'s phone row once its demo works in the lab, the four round twos still unruled (`seed-avatar`, `app-door`, `pricing-page`, `demo-event`), `toasts`, and the identity board's three configured Auth rate limits (Dashboard, Authentication, Rate Limits), his read. The sixth batch's six lanes run on the seats; the queue is in `tracks/orchestrator.md`. A question an earlier ruling reaches is badged in its walk (`overtaken`): an answer overrides, "The ruling stands" records it. On the alias for his eye: the profile and the guest list, the chrome, the events hub and the four type pages (batch four's wiring, the calls his to overrule in the CHANGELOG), and before them `/how-it-works` with the home's stepper and every failure page. The overnight round's red-team is complete: the signed-in half (the host event page at 1440 and 375, the guest album as
the host at 1280 / 1512 / 1920 / 375, the dashboard) ran in his Chrome session on 2026-09-19 and was clean. Calls his to overrule from earlier lanes: the home's live
demo as the paper chapter's opener and the album heading at `lg`; the ghost's fade; type inside a picture counts as
depicted; the album at 896. Assets open: rows 15, 16, 19 and 23 to 25 (26 withdrawn: `door` won). The launch-gated tasks are the ROADMAP's Launch checkpoint.
