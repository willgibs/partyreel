# The asset log

> **ROLE:** Will's running list of every asset a round has requested, so he can produce them in
> parallel to reviews and tasks and nothing asked for is lost. **BELONGS HERE:** one row per asset:
> what, the exact spec, who asked (the track), the stand-in it replaces by id, where it stands, and
> where the file is once it exists. **NOT HERE:** why a design needs it (the manifest and the board
> say that), or the wiring itself (the wiring round's commit). **GROWS BY:** the Orchestrator folds
> each manifest's "Assets requested from Will" line at integration; Will marks a row `delivered` and
> fills `where`; the wiring round marks it `wired` and deletes the stand-in. Rows are never deleted;
> a wired row is the record of what replaced what.

**How to deliver.** Drop the files in the inbox outside the repo,
`/Users/gibby/local/ai/partyreel-assets/<row>/` (delivered media is large and unoptimised; only what
a wiring round ships enters `public/marketing/`, sized and named by `marketing-media.ts`), then mark
the row `delivered` here or say so in chat. Either way the Orchestrator commits the log. Will's edits
to this file are safe beside the Orchestrator's (it stages explicitly and reads the file before every
edit); no agent edits it (asks go in the manifest's Handoff, one bullet per asset).

**Status:** `requested` → `delivered` → `wired`; `parked` when the round that asked was not ruled in but
the asset still serves a queued idea; `withdrawn` when nothing uses it (the row stays as the record).

| # | what | spec | asked by | replaces (id) | status | where |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | The hero film | 15 to 20 s of real event moments, fast cuts on the beat, 12 to 18 shots (a toast, confetti, the dance floor from above, sparklers, hands in the air, the cake, a phone held up filming, a first dance; nothing that needs a face in focus, the type sits over the left half). Graded dark and warm with the left 55% of frame in the lower third of the range in every shot. Masters 1920x1080 and a 1080x1920 crop of the same edit; muted, no audio track. Files: H.264 mp4 under 1.5 MB at 1080, a VP9 webm beside it, a poster from the FIRST graded frame at both aspects under 120 KB each. Plus the cut list: shot boundary times in seconds. | `hero-reel` (the reel concept) | `hero-candidate-02` (8.25 s, four shots) and its poster; the phone crop replaces `hero-candidate-01` in that concept | parked (the reel hero was not ruled in, 2026-09-14; the film serves the queued "Watch your event highlights" video card) | |
| 2 | 24 event photographs, squares | 512x512, one grade, 6 to 35 KB webp each, across weddings, birthdays, corporate and festivals; framed tight enough to read at 120 px (a face, two hands, a glass, a sparkler, a first dance), never a wide room shot; derived from row 7 (crops of 24 of the 36 masters) rather than shot separately | `hero-source` (the source concept) | the 12 landscape stand-ins the corridor cycles (`FRAMES` in `sandbox/home-hero/shared.tsx`) | requested | |
| 3 | 36 event photographs, one grade | 1600 px long edge, a third of them portrait; the field places them in 3:2, 16:9, 1:1, 4:3, 4:5, 3:4 and 2:3 boxes, so a frame that exists only at 3:2 crops hard in the tall slots; 3 of the 36 show a guest holding a phone up at the event | `hero-gathering` (the gathering concept) | all twelve manifest stills: `wedding-golden`, `party-dj`, `reception-table`, `festival-lights`, `wedding-petals`, `concert-confetti`, `wedding-toast`, `festival-crowd`, `wedding-rings`, `reception-hall`, `party-balloons`, `wedding-arch` | parked (the gathering was not ruled in, 2026-09-14; the three phone-up frames may serve `hero-scan`, and a portrait set may serve `hero-burst` or `hero-river`; the media-kit track's plan absorbs the rest) | |
| 4 | 8 vertical clips with posters | 3 to 5 s each, 1080x1920, silent, each with its own poster at 1080x1920 (the clip's first frame); three run at once on desktop, two on phone | `hero-gathering` | the three `currentTime` ranges cut out of `hero-candidate-01`, which share one poster today | parked (revives with row 7: the same shoot; the gathering itself was not ruled in) | |
| 5 | The demo event's curated folder | the photographs and clips the demo event is seeded from (`scripts/seed-demo-event.mjs`, idempotent by replacement); the live QR on every hero board points at this event | the demo seed (STATUS, parked item 9) | the seed's current fixtures | requested | |
| 6 | A per-batch OK on the 12 marketing stills | the 12 `MARKETING_IMAGES` all carry `license: unsplash (provenance unverified)`; the `media-kit` track surveyed the sources and staged 8 CC0 candidates under `public/design/media-kit/` with `provenance.json`; its recommendation is a dated bridge on the blog cover pool only, never the hero. ★ Unsplash's terms exclude recognizable people from the license and all 12 stills are full of them; 11 of the 12 are the blog's cover pool (23 posts, their OG cards, the RSS enclosures) | the marketing media batch (STATUS, parked item 7; the count was recorded as 4, the manifest has 12) | the 12 stills themselves | requested | |
| 7 | The kit, 36 masters | 36 event photographs, six per vertical (weddings, birthdays, corporate, conferences, festivals, trips), 1600 px long edge, a third portrait, one grade; 3 of the 36 show a guest holding a phone up; framed to read at 120 px and to survive a 22 to 78 percent crop; the shot lists are `docs/specs/media-kit.md` 5.1. Four of the 36 are the palette board's hard cases (one high key, a white dress against a white wall; one low key, a dance floor lit by one lamp; one candle-warm; one stage-cool), landscape, because a ramp is only ever wrong against media that fights it. The 24 squares (row 2), the 8 clips (row 4) and the film (row 1) are crops and cuts of this one shoot, not separate deliveries | `media-kit` (the review wave) | all twelve `MARKETING_IMAGES` by id (`wedding-golden`, `reception-table`, `party-balloons`, `concert-confetti`, `wedding-rings`, `reception-hall`, `party-dj`, `wedding-toast`, `festival-lights`, `festival-crowd`, `wedding-arch`, `wedding-petals`) | requested | |
| 8 | A hand-and-phone cutout | PNG with alpha, 1200 px on the long edge, the SCREEN AREA fully transparent so the live viewfinder composes underneath; shot from just behind the holder's shoulder with the phone held up and angled away to the right, in low warm event light so the body is nearly a silhouette with one highlight along the edge; two variants, a one-handed grip and a two-handed one | `hero-scan` (round three, variation 2) | the drawn device (`.hhc-phone` in `sandbox/home-hero/scan.tsx`) | requested | |
| 9 | 11 portrait crops of the squares | 11 of the 24 squares (row 2) also as 4:5 portrait crops, 512x640, the same photograph recropped, the same grade, for the third of the burst's field that lays out vertical (11 4:5 slots at 1440 after round three's balance pass, so 11 rather than 8 or one frame doubles) (guests shoot vertical; a field of nothing but squares reads as a deck of cards) | `hero-burst` (round three, variation 3) | the square box on that third of the field | requested | |
| 10 | A grain tile | seamless monochrome noise, 256x256, PNG-8, fine grain (about 1 px), neutral, mean 50 percent grey, used at about 5 percent over the light so the aurora stops banding | `light` (the review wave) | the inline feTurbulence stand-in in `sandbox/light/board.css` (`[data-lgt-grain]`) | requested | |
| 11 | A worst-case pair of overlapping photographs | two images whose touching edges are both dark and low contrast (a night reception, a dim dance floor), 1200 px long edge, JPG, so the depth cue is judged against the case it exists for rather than a lucky one | `light` (the review wave) | the `reception-hall` + `wedding-toast` pair in `sandbox/light/depth.tsx` | requested | |
| 12 | 12 event photographs, portraits | 4:5 at 720x900, one grade, 15 to 60 KB webp each, from the same shoot as the squares, framed tight enough to read at 110 px; about 45 percent of the river's stream runs portrait because that is what a guest's phone shoots (row 3's parked portrait third would also serve, and may be cheaper to unpark) | `hero-river` (round three, variation 4) | the portrait cards (`wf` 0.8) in `sandbox/home-hero/river.tsx`'s `CARD_POOL`, cropped hard from landscape today | requested | |
