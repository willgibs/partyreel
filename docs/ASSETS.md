# The asset log

> **ROLE:** Will's running list of every asset a round has requested, so he can produce them in
> parallel to reviews and tasks and nothing asked for is lost. **BELONGS HERE:** one row per asset:
> what, the exact spec, who asked (the track), the stand-in it replaces by id, where it stands, and
> where the file is once it exists. **NOT HERE:** why a design needs it (the manifest and the board
> say that), or the wiring itself (the wiring round's commit). **GROWS BY:** the Orchestrator folds
> each manifest's "Assets requested from Will" line at integration; Will marks a row `delivered` and
> fills `where`; the wiring round marks it `wired` and deletes the stand-in. Rows are never deleted;
> a wired row is the record of what replaced what.

**How to deliver.** Drop the files in the inbox outside the repo,
`/Users/gibby/local/ai/partyreel-assets/<row>/` (delivered media is large and unoptimised; only what
a wiring round ships enters `public/marketing/`, sized and named by `marketing-media.ts`), then mark
the row `delivered` here or say so in chat. Either way the Orchestrator commits the log. Will's edits
to this file are safe beside the Orchestrator's (it stages explicitly and reads the file before every
edit); no agent edits it (asks go in the manifest's Handoff, one bullet per asset).

**Status:** `requested` → `delivered` → `wired`.

| # | what | spec | asked by | replaces (id) | status | where |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | The hero film | 15 to 20 s of real event moments, fast cuts on the beat, 12 to 18 shots (a toast, confetti, the dance floor from above, sparklers, hands in the air, the cake, a phone held up filming, a first dance; nothing that needs a face in focus, the type sits over the left half). Graded dark and warm with the left 55% of frame in the lower third of the range in every shot. Masters 1920x1080 and a 1080x1920 crop of the same edit; muted, no audio track. Files: H.264 mp4 under 1.5 MB at 1080, a VP9 webm beside it, a poster from the FIRST graded frame at both aspects under 120 KB each. Plus the cut list: shot boundary times in seconds. | `hero-reel` (the reel concept) | `hero-candidate-02` (8.25 s, four shots) and its poster; the phone crop replaces `hero-candidate-01` in that concept | requested | |
| 2 | 24 event photographs, squares | 512x512, one grade, 6 to 35 KB webp each, across weddings, birthdays, corporate and festivals; framed tight enough to read at 120 px (a face, two hands, a glass, a sparkler, a first dance), never a wide room shot | `hero-source` (the source concept) | the 12 landscape stand-ins the corridor cycles (`FRAMES` in `sandbox/home-hero/shared.tsx`) | requested | |
| 3 | 36 event photographs, one grade | 1600 px long edge, a third of them portrait; the field places them in 3:2, 16:9, 1:1, 4:3, 4:5, 3:4 and 2:3 boxes, so a frame that exists only at 3:2 crops hard in the tall slots; 3 of the 36 show a guest holding a phone up at the event | `hero-gathering` (the gathering concept) | all twelve manifest stills: `wedding-golden`, `party-dj`, `reception-table`, `festival-lights`, `wedding-petals`, `concert-confetti`, `wedding-toast`, `festival-crowd`, `wedding-rings`, `reception-hall`, `party-balloons`, `wedding-arch` | requested | |
| 4 | 8 vertical clips with posters | 3 to 5 s each, 1080x1920, silent, each with its own poster at 1080x1920 (the clip's first frame); three run at once on desktop, two on phone | `hero-gathering` | the three `currentTime` ranges cut out of `hero-candidate-01`, which share one poster today | requested | |
| 5 | The demo event's curated folder | the photographs and clips the demo event is seeded from (`scripts/seed-demo-event.mjs`, idempotent by replacement); the live QR on every hero board points at this event | the demo seed (STATUS, parked item 9) | the seed's current fixtures | requested | |
| 6 | A per-batch OK on the 12 marketing stills | the 12 `MARKETING_IMAGES` all carry `license: unsplash (provenance unverified)`; the `media-kit` track surveys the sources and stages a candidate batch; the OK (or the replacement batch, rows 2 to 4) settles them | the marketing media batch (STATUS, parked item 7; the count was recorded as 4, the manifest has 12) | the 12 stills themselves | requested | |
| 7 | A licensed media kit | photographs by vertical (weddings, birthdays, corporate, festivals, trips, conferences), vertical clips, posters, under licenses we can name, or generated by Will; the `media-kit` track writes the plan and the shot lists (`docs/specs/media-kit.md`) | `media-kit` (the review wave) | every marketing stand-in over time | requested | |
