# The asset log

> **ROLE:** Will's running list of every asset a round has requested, so he can produce them in
> parallel to reviews and tasks and nothing asked for is lost. **BELONGS HERE:** one row per asset:
> what, the exact spec, who asked (the track), the stand-in it replaces by id, where it stands, and
> where the file is once it exists. **NOT HERE:** why a design needs it (the manifest and the board
> say that), or the wiring itself (the wiring round's commit). **GROWS BY:** the Orchestrator folds
> each manifest's "Assets requested from Will" line at integration; Will marks a row `delivered` and
> fills `where`; the wiring round marks it `wired` and deletes the stand-in. Rows are never deleted;
> a wired row is the record of what replaced what.

**How to deliver.** Drop the files in the inbox outside the repo,
`/Users/gibby/local/ai/partyreel-assets/<row>/` (delivered media is large and unoptimised; only what
a wiring round ships enters `public/marketing/`, sized and named by `marketing-media.ts`), then mark
the row `delivered` here or say so in chat. Either way the Orchestrator commits the log. Will's edits
to this file are safe beside the Orchestrator's (it stages explicitly and reads the file before every
edit); no agent edits it (asks go in the manifest's Handoff, one bullet per asset).

**Status:** `requested` → `delivered` → `wired`; `parked` when the round that asked was not ruled in but
the asset still serves a queued idea; `withdrawn` when nothing uses it (the row stays as the record).

**The source, from 2026-09-18:** every image and every video on the site is generated in one
Higgsfield month once more of the site is shaped (ROADMAP, major overhauls). Nothing is shot or
licensed, and no row tracks where an image came from (Will: "if we're using images, it inherently
means we have the licenses to do so"). **An ask names the slot, never the picture:** where the frame
sits, its size and aspect, the crops it must survive and where type lands on it. The agent that runs
the month researches Higgsfield's tooling first and writes every final prompt itself, so the creative
detail in older rows is context for it, not a prompt.

| # | what | spec | asked by | replaces (id) | status | where |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | The hero film | 15 to 20 s of real event moments, fast cuts on the beat, 12 to 18 shots (a toast, confetti, the dance floor from above, sparklers, hands in the air, the cake, a phone held up filming, a first dance; nothing that needs a face in focus, the type sits over the left half). Graded dark and warm with the left 55% of frame in the lower third of the range in every shot. Masters 1920x1080 and a 1080x1920 crop of the same edit; muted, no audio track. Files: H.264 mp4 under 1.5 MB at 1080, a VP9 webm beside it, a poster from the FIRST graded frame at both aspects under 120 KB each. Plus the cut list: shot boundary times in seconds. Made in the Higgsfield month if it takes on video. | `hero-reel` (the reel concept) | `hero-candidate-02` (8.25 s, four shots) and its poster; the phone crop replaces `hero-candidate-01` in that concept | parked (the reel hero was not ruled in, 2026-09-14; the film serves the queued "Watch your event highlights" video card) |  |
| 2 | 34 event photographs, squares | 512x512, one grade, 6 to 35 KB webp each, across weddings, birthdays, corporate and festivals; framed tight enough to read at 120 px (a face, two hands, a glass, a sparkler, a first dance), never a wide room shot, and each survives a centre crop to 4:5 and 4:3 (half the corridor is portrait now); generated in the Higgsfield month (row 7, the shoot they were to be cropped from, was withdrawn 2026-09-17). The count moved 24 to 34 in round four (`hero-source`): the corridor holds 17 frames a side at 1440 and the two arms' windows are disjoint only when the set is at least twice that pool; re-derive it if the cadence changes | `hero-source` (the source concept) | the 12 landscape stand-ins the shipped hero's band cycles (`FRAMES` in `sandbox/home-hero/shared.tsx`, read by `src/components/marketing/sections/home/hero-stream.ts`; 18 needed for no photograph on screen twice) | requested |  |
| 3 | 36 event photographs, one grade | 1600 px long edge, a third of them portrait; the field places them in 3:2, 16:9, 1:1, 4:3, 4:5, 3:4 and 2:3 boxes, so a frame that exists only at 3:2 crops hard in the tall slots; 3 of the 36 show a guest holding a phone up at the event | `hero-gathering` (the gathering concept) | all twelve manifest stills: `wedding-golden`, `party-dj`, `reception-table`, `festival-lights`, `wedding-petals`, `concert-confetti`, `wedding-toast`, `festival-crowd`, `wedding-rings`, `reception-hall`, `party-balloons`, `wedding-arch` | parked (the gathering was not ruled in, 2026-09-14; the three phone-up frames may serve `hero-scan`, and a portrait set may serve `hero-burst` or `hero-river`; the Higgsfield month makes whatever is still wanted) | |
| 4 | 8 vertical clips with posters | 3 to 5 s each, 1080x1920, silent, each with its own poster at 1080x1920 (the clip's first frame); three run at once on desktop, two on phone | `hero-gathering` | the three `currentTime` ranges cut out of `hero-candidate-01`, which share one poster today | parked (revives with the Higgsfield month if it takes on video; the gathering itself was not ruled in) |  |
| 5 | The demo event's curated folder | the photographs and clips the demo event is seeded from (`scripts/seed-demo-event.mjs`, idempotent by replacement); the live QR on every hero board points at this event | the demo seed (STATUS, parked item 9) | the seed's current fixtures | requested | |
| 6 | A per-batch OK on the 12 marketing stills | the 12 `MARKETING_IMAGES` stand-ins, copied in from a dev pack as placeholders; 11 of the 12 are the blog's cover pool (23 posts, their OG cards, the RSS enclosures) and six sit in the chrome of every marketing page. The `media-kit` board surveyed stock to replace them and was killed for generation instead | the marketing media batch (STATUS, parked item 7; the count was recorded as 4, the manifest has 12) | the 12 stills themselves | withdrawn (Will, 2026-09-17: the Higgsfield month replaces the 12 whole, and no agent tracks where an image came from) |  |
| 7 | The kit, 36 masters | 36 event photographs, six per vertical (weddings, birthdays, corporate, conferences, festivals, trips), 1600 px long edge, a third portrait, one grade, shot in one night at an event we host; four were the palette board's hard cases (high key, low key, candle-warm, stage-cool), and the squares (row 2), the clips (row 4) and the film (row 1) were to be cut from the same shoot | `media-kit` (the review wave) | all twelve `MARKETING_IMAGES` by id (`wedding-golden`, `reception-table`, `party-balloons`, `concert-confetti`, `wedding-rings`, `reception-hall`, `party-dj`, `wedding-toast`, `festival-lights`, `festival-crowd`, `wedding-arch`, `wedding-petals`) | withdrawn (Will, 2026-09-17: no shoot, and the call sheet was deleted rather than kept as a brief; the Higgsfield month writes its own) |  |
| 8 | A hand-and-phone cutout | PNG with alpha, 1200 px on the long edge, the SCREEN AREA fully transparent so the live viewfinder composes underneath; shot from just behind the holder's shoulder with the phone held up and angled away to the right, in low warm event light so the body is nearly a silhouette with one highlight along the edge; two variants, a one-handed grip and a two-handed one | `hero-scan` (round three, variation 2) | the drawn device in the retired scan hero (git keeps `sandbox/home-hero/scan.tsx`) | withdrawn (the scan retired with the home hero's round-five ruling, 2026-09-16: the source won; nothing draws a phone in hand now, and the row stays as the record) |  |
| 9 | 11 portrait crops of the squares | 11 of the 34 squares (row 2) also as 4:5 portrait crops, 512x640, the same photograph recropped, the same grade, for the third of the burst's field that lays out vertical (11 4:5 slots at 1440 after round three's balance pass, so 11 rather than 8 or one frame doubles) (guests shoot vertical; a field of nothing but squares reads as a deck of cards) | `hero-burst` (round three, variation 3) | the square box on that third of the field | requested |  |
| 10 | A grain tile | seamless monochrome noise, 256x256, PNG-8, fine grain (about 1 px), neutral, mean 50 percent grey, used at about 5 percent over the light so the aurora stops banding | `light` (the review wave) | nothing now: the board's inline stand-in left with its composer (round eight) | superseded by row 15, the same ask from the same board in the later wave; deliver against 15 | |
| 11 | A worst-case pair of overlapping photographs | two images whose touching edges are both dark and low contrast (a night reception, a dim dance floor), 1200 px long edge, JPG, so the depth cue is judged against the case it exists for rather than a lucky one | `light` (the review wave) | was `sandbox/light/depth.tsx`, deleted with the board on 2026-09-17 | superseded by row 16; deliver against 16 | |
| 12 | 12 event photographs, portraits | 4:5 at 720x900, one grade, 15 to 60 KB webp each, from the same generated set as the squares, framed tight enough to read at 110 px; about 45 percent of the river's stream runs portrait because that is what a guest's phone shoots (row 3's parked portrait third would also serve, and may be cheaper to unpark) | `hero-river` (round three, variation 4) | the band's 4:5 frames in the shipped hero (`hero-stream.ts`), where a 512 square is cropped to 4:5 and upscaled today | requested | |
| 13 | The bridge, bought: one month of Unsplash+ and three iStock frames | $20 for the month, $36 for the three conference-room frames, $56 in total | `media-kit` (round four: the only line completed with a card instead of a camera) | the staged CC0 bridge, deleted with the board | withdrawn (Will, 2026-09-17, `spend=hold`: nothing is bought, and stock is off the table) | |
| 14 | A dark, low-key event photograph for the menu ground | 1600 px long edge, landscape, one grade: a dance floor lit by a single lamp; the ground the floating-surfaces catalog opened its seven menus on | `floating-surfaces`, **retired 2026-09-17**, so the asker is gone | the nine mid-key marketing photographs that catalog cycled, all bright enough to flatter a translucent pane | parked (the media kit's shot list it was folded into was deleted with that board, 2026-09-17); still wanted from the Higgsfield month, because the banked Glass exploration needs exactly this frame to judge a translucent pane against |  |
| 15 | A grain tile, so the aurora stops banding | Seamless monochrome noise, 256x256 PNG-8, one-pixel grain, neutral, mean 50 percent grey; used at 5 percent and laid out at 128 CSS px on a 2x screen | `light`, retired 2026-09-17; the Aurora it serves is shipped | nothing now: the tile lands on `SectionLight` (`src/components/marketing/system/section-light.tsx`), which ships without a dither | requested (the live row; 10 is its duplicate) |  |
| 16 | A worst-case pair of overlapping photographs | Two images whose touching edges are both dark and low contrast (a night reception, a dim dance floor), 1200 px long edge, JPG, so the lift is judged against the case it exists for | `light`, retired 2026-09-17; the lift it tests is shipped (`--shadow-lift`) | the marketing pool's `wedding-rings` over `reception-table`, now in the Library's Elevation legend (`(shell)/library/foundations/elevation-legend.tsx`) | requested (the live row; 11 is its duplicate) |  |
| 17 | A worst-case tile set for the gallery gap | four photographs with near-white, bright edges (a tablecloth, an overexposed sky, a white dress), 1200 px long edge, JPG, so a one-pixel corner hole is judged at maximum contrast | `rounding` (round six, re-asked at round seven) | the nine brightest edges in the marketing pool, which the gap step borrows (`sandbox/rounding/specimens.tsx`) | withdrawn (Will ruled `gap=pinned` on 2026-09-18 and the board retired: the gap is `max(3px, var(--radius-tile))`, 4px, checked for holes on the demo album's real photographs) | |
| 18 | The v1 wordmark | Will's own: `partyreel-wordmark-v1.svg`, 308 by 64, one path, drawn white on a dark artboard | Will (chat, 2026-09-17: "add to library and replace our placeholder") | the placeholder lockup in `src/components/shared/logo.tsx` (the Aperture tile beside "Partyreel" set in the heading face) | wired | `src/lib/brand/wordmark.ts` (the path), `Logo`, the social card; his source file stays at `/Users/gibby/local/files/partyreel-wordmark-v1.svg` |
| 19 | The v1 icon (the mark) | Will's own, to come ("I'll upload new v1 icon separately later once complete"); SVG, square, legible at 16 px, with a mono version for footage | Will (chat, 2026-09-17) | the Aperture stand-in: `Logo markOnly`, `src/app/icon.svg`, `favicon.ico`, `apple-icon.png`, `manifest.ts`'s icons, the reel watermark's badge in `src/lib/reel/engine/canvas2d.ts` (the REAL-LOGO seam), the press kit's five marks and app icon | requested | |
| 20 | Room frames for the switching photograph backdrop | six landscape photographs at 2880 px wide or more, each with a QUIET middle third and no blown highlight there (the band the copy sits in); the pool a photograph section cycles; a Higgsfield slot, asked by slot never by picture | `cursor-backdrop` (2026-09-18) | the 700 to 900 px stand-ins the board upscales 1.6x to 2.1x | requested | |
| 21 | Trail frames | twenty-four distinct portrait photographs (3:4) for the image trail's ring, so at the densest option no photograph is on screen twice; a Higgsfield slot | `image-trail` (2026-09-18) | the twelve stand-ins filling 24 slots | requested | |
