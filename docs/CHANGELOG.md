# Partyreel — Changelog (the last two rounds)

> ROLE: what shipped in the current round and the one before, with the commits and the verification.
> BELONGS HERE: at most two dated entries, newest first, each at most 160 lines. · NOT HERE: how the
> system works now (→ [`systems/`](systems)), what is next (→ [`ROADMAP.md`](ROADMAP.md)), the live
> state (→ [`STATUS.md`](STATUS.md)). GROWS BY: a new entry at a round's close, and the third-oldest
> entry deleted in the same commit. Everything older is in git: `git log --oneline` for the commits,
> `git show 932fdee9:docs/CHANGELOG.md` for the last full archive (5,495 lines, 2026-07-02 to 09-16),
> `git show dd77fc9e:docs/CHANGELOG.md` for the Library x Lab round's entry, `git show 52e9afa2:docs/CHANGELOG.md` for the revamp's,
`git show d2db2629:docs/CHANGELOG.md` for the stepped review round's, `git show 449d9b52:docs/CHANGELOG.md` for the wind-down's, `git show 22438704:docs/CHANGELOG.md` for the ladders-and-the-dock round's, `git show b30445d9:docs/CHANGELOG.md` for the overnight round's, `git show 69a9a177:docs/CHANGELOG.md` for the morning sitting's round's, `git show a79cd55c:docs/CHANGELOG.md` for the evening sitting's (batch four), `git show 08967867:docs/CHANGELOG.md` for the night sitting's (batch five), `git show 87ede4da:docs/CHANGELOG.md` for the small hours' (batch six's first cut). `git show ac325953:docs/CHANGELOG.md` for the small hours' second entry (the round twos and the queue, 2026-09-20),

---

## 2026-09-22 — The reel round: Will's own project as the production round (`17f17e57` onward)

**What Will did.** At about 13:00 EDT, with the identity round whole on the alias, he brought his own project: "let's rework the
reel feature concept entirely, I feel like it's still missing the mark as a flagship feature." His reasons, his direction and
his eight answers across two rounds of questions are verbatim in rulings.md ("the reel, reconceived"): the reel becomes a live,
looping montage of everything the album shows, alive from the third item, no host action, no stored file, the host's mood as the
default with a viewer's own switch, a first-class venue screen, and a cut anyone makes on-device (saved or shared as a file, never
stored; "Add to the album" on a paid event as an ordinary video upload the live reel skips); video plays a range-fetched window
of the original decoded on the viewer's device behind an Include videos toggle; hide and Review are the only curation; a per-event
switch default on. The plan was approved at about 14:45 EDT after two fresh-context reviews he asked for; it runs as two engine
lanes now beside six lab boards in two waves, the wiring after his desk review, the old reel live on the alias until one build
replaces it. His standing rule of 2026-09-20 (no new board until the desk is closed) is superseded by this round on his own word.

**The record.** The ruling in rulings.md with the supersessions named; the ROADMAP's reel bullet reconceived; the `reel-studio`
board retired unreviewed (its questions reshaped into `reel-front` and `reel-cut`); six standing asks badged by the first chat
ruling to badge; the reel's own system doc to be born at the wiring's record.

- **The reel round opened (Will's own project, 2026-09-22 ~13:00 to ~14:45 EDT; his brief and eight answers verbatim in rulings.md "the reel, reconceived"; the record `17f17e57`, the cut `ef1c4b51`)**: the reel is reconceived as a live, looping montage of the visible album from the third item (no host action, no stored file, the host's mood as the default and a viewer's own switch, a first-class venue screen) with a cut anyone makes on-device (saved or shared as a file, never stored; "Add to the album" on a paid event as an ordinary video upload the live reel skips) and video as a range-fetched window of the original decoded on the viewer's device behind an Include videos toggle. On the tree at the record: the `reel-studio` board RETIRED UNREVIEWED (the registry, the boards map, `DESK_ORDER`, its folder; git keeps it at `90f29be4`; `door` and `guests` reshape into `reel-front`, `room`, `styles`, `moments`, `blocked` and `wait` into `reel-cut`, `sharing` removed as valueless: a board whose product no longer exists retires unreviewed, its live questions reshaped into the boards that replace it); six standing asks badged in `overtaken.ts` by the first chat ruling to badge (`media-viewer.link`, `.video`, `.wayout`; `host-curation.arrivals`, `.count`; `press-page.the-facts`), the desk test's three-outcome case made vacuous under three badges on its chosen board; the ROADMAP's reel bullet reconceived with the round's deferred lines. Cut at the record: `reel-engine-live` (Opus, :3132) and `reel-engine-video` (Opus, :3137), the two engine lanes with lab harnesses; `reel-view` (Sonnet, :3138), `reel-front` (Sonnet, :3139) and `reel-screen` (Opus, :3131), three new lab boards; wave 2 (`reel-cut`, `reel-host`, `reel-story`) briefed and registered, cut as seats free. The old reel stays live on the alias until the wiring round's one build replaces it; the bucket's CORS change for the video lane is Will's command.

- **`reel-engine-video` on the tree at `31171495`** (2026-09-22, handed off `3183921b` by Opus on :3137; gate 108 green with 442 smoke checks; the reel round's first engine lane, a library with a lab harness and no production surface; Will's ruling: "Range-window decode on device" behind an "Include videos" toggle): `src/lib/reel/engine/video/` (a window reader over mediabunny's `Input`/`UrlSource` with `cache: no-store` and `mode: cors`, an 8 MiB cache, parallelism 1, two quick retries then fail; a size read that doubles as the range probe; `canDecode`; a `CanvasSink` into an 8-frame ring paced by the draw; the generator returned and the `Input` disposed in a `finally`; every failure resolving to the poster, with `possibleExpiry` on a statusless rejection; a deck of at most two readers reopening on a new presign), `budget.ts` (the window ladder, a per-play ledger, the per-clip K-loop cadence), `ladder.ts` (the whole fallback order, one pure call), `prepare-frame.ts` (the one place the ladder runs; the live player's and the cut's shared wiring); `reel-types.ts` gained `ReelClip.video` and a SYNCHRONOUS `ReelVideoSource.frameAt` (the draw never awaits; `registry.ts`'s pooled-scratch invariant stands), `assets.ts` mirrors it poster-first, `styles/mood.ts` draws the decoded frame in the poster's place (a posterless video too), `encode.ts` awaits one `prepareFrame` before each draw. The harness at `/design/lab/tools/reel-video` over a real mov, webm and portrait mp4 served by a range route of its own (206 with `Content-Range`): 180 frames decoded a six-second window, 0.52 to 0.66 MB a play, four requests for the mov over 77 seconds, live readers never above two, the JS heap flat or falling across nine minutes, the toggle taking readers to zero. One call his to overrule: the decode is aspect-preserving with its long edge capped at the composition's short side rather than cover-cropped at composition size (a pre-cropped decode would double-crop a contained clip and disagree with its own poster). The R2 CORS precondition is still Will's command: until it lands an alias range read answers a size the browser will not report and the reader draws the poster, a degradation pinned by a test, never a break.

- **`reel-view` on the desk at `b063f1cc`** (2026-09-22, handed off `785f9797` by Sonnet on :3138; gate 109 green with 451 smoke checks; the reel round's first board): the reel's full-screen view as a catalog of eight asks on the wedding album's world, every option a live `CanvasReelPlayer` drawing the real engine over the fixture clips (`chrome`: nothing until the pointer moves, a thin bar that fades, controls always on; `controls`: the set and its order as three arrangements; `arrival`: the just-added beat as a caption, a chip, nothing; `tap`: the picture opens the item in the viewer, pauses, nothing; `posture`: one composition following the viewport or the reel's own aspect letterboxed; `pacing`: three hand holds as running takes; `loop`: how a new take announces itself; `reduced`: what reduced motion starts on), at 1440 and 375 (`lab:demo` 8 steps, the tallest `posture` at 1.6 screens); registered after `media-viewer` and placed first of the reel boards at the merge (the lane's sync carried the three identity boards' collision on the registry's head, concatenated by hand with both orders kept). Four calls his to overrule beyond the asks: Close in its own corner; the timer-driven beats; the three pacing numbers; the Cinematic stand-in mood. Look at first: `chrome` (move the pointer over the bare option) and `posture`. Nothing here wires production; `reel-guest-wiring` builds the verdict.

- **`reel-screen` on the desk at `ac325953`** (2026-09-22, handed off `c060f326` by Opus on :3131; gate 110 green with 460 smoke checks): the venue screen as a catalog of eight asks drawn on a 1920 by 1080 wall with 1440 on the knob (`qr`: the code's corner and size, 12 to 15 percent of the width across the flip; `name`; `caption`; `pacing` on three running walls; `idle`; `start`: the one-tap plate that takes the gesture for fullscreen and the wake lock; `review`: seven waiting for the host, the shared fixture's own queue; `open`: the hub's button, a screen link drawn as a later option, the settings sheet). The kit draws no canvas past 1440 (`CANVAS.desktop` in `stage.tsx`), so the board carries its own 1920 `Frame` in `wall.tsx` (a Frame, not a div, because only a real 1920 viewport resolves the fluid type ladder's wall sizes); a deferred line says what lifting the cap would take. Registered after `identity-profile` and placed among the reel boards at the merge. Four calls his to overrule: the wall wears Sunset rather than Cinematic (Cinematic letterboxes a 16:9 wall and would judge every corner over black bars, a finding on its own); the side panel drops the event's name so `name` keeps one variable; Review holds seven, not the brief's three; seven asks on held engine frames, `pacing` alone on real rAF walls. Look at first: `qr` flipped between 1920 and 1440, then `pacing` with motion allowed. Nothing here wires production.

- **`reel-engine-live` on the tree at `d7fc43cb`** (2026-09-22, handed off `e2b0427e` by Opus on :3132; gate 111 green with 460 smoke checks; the reel round's second engine lane, a library with a lab harness and no production surface): `src/lib/reel/live/` (`items`, a structural supertype of `GridMedia` so the provider hands its payload straight in; `take`, a seeded order per loop from the quick-add brain run in passes; `pacing`, one surface factor on the hold, every transition and the video window; `window`, a plan per window overlapping by one clip; `source`, the `ClipSource`), `engine/player-live.tsx` (a monotonic clock that never resets, two windows prefetched through retained bitmaps, a tick that survives a throwing draw, an immediate cutaway), `engine/layout.ts` (the hold guard on the sum of adjacent gaps, a proven no-op for all eight moods at both surfaces, and an `indexOffset` that makes a window the exact slice of the loop's plan), `engine/asset-cache.ts` (retain and release refcounts); Include videos ALIVE through the video lane's `createVideoPlayback` over one session deck and ledger; the harness at `/design/lab/tools/reel-live`. The soak found three stalls of one shape (clock running, picture frozen), each pinned with a failing negative control: a prefetch hole nothing refilled, a prefetch eating the arrival queue mid-transition, a slot chain pointing at a released slot; the final run drew 14,241 frames unbroken across a loop roll with 24 acts landing (clock backward 0, failures 0, heap flat at 40 to 74 MB, 44 video clips with real range-decoded motion; a drop leaves in one frame, an arrival is on screen in 9 to 12). Two calls his to overrule, each keeping the brief's intent over its letter: the window seed is the loop's plus an index offset rather than a per-window hash (a per-window seed re-rolls the pan and the shared clip's scale would jump about five percent at every handover); a splice reopens the current window on the clip playing now (mutating only the upcoming window put an upload up to a whole window away, 11.4 seconds measured). The 20-minute wall-clock soak was not reached because the shared browser pane was navigated away twice; the Handoff says so. Announced: the `ClipSource` and the live player's props, which the wiring codes to.

- **`reel-cut` on the desk at `fbc15ae7`** (2026-09-22, handed off `5abe8522` by Opus on :3134 at 15:50 EDT, a minute before the Claude app update killed the running lanes; gate 112 green with 470 smoke checks): the creator, from the reel to a cut, as a catalog of nine asks on Priya's paid event, every frame the engine over the twelve bootstrap stills, reshaping the retired `reel-studio` board's room, styles, moments, blocked and wait (the lane's recommendations: `entry` a room of its own the cut slides into, since a sheet over the view would put two Style controls two inches apart; `room` the bench, the open panel beside the cut at a laptop, measured covering none of it; `looks` the wall of fourteen engine-drawn tiles; `moments` a pool under the cut with the fill knob live at 8, 5 and 23; `blocked` the tile saying it itself; `wait` the frame stacking and counting down; `finish` Share leading, with a plan knob for Add to the album; `mark` the real free mark stamped by the dispatch layer with one line; `noencode` one honest line where the button was). Three findings fixed and kept as rules for the next board over this world: a full-width panel caps its content (a 560 px reading column); a tooltip cannot live inside the tile that clips its photograph; a quiet door loses its icon and a type step at 375. Calls his to overrule: the paid event and Priya as the maker, the shipped tray of five drawn but not asked, three placeholder fill words, the export verb "Make it", the mark's line "Free cuts carry the small mark and run to 30 seconds. Pro removes both." The console error it reported on every lab route (`window.__reelLive is not a function`) is the live harness's dev-only console hook (`reel-live/harness.tsx`) polled by that lane's soak in the shared pane, not a product defect. Look at first: `room` at 1440 with the sidebar collapsed, then `mark` and `wait`. Nothing here wires production.

- **`reel-front` on the desk at `03e7e599`** (2026-09-22, handed off `fc858e91` by Sonnet on :3139 after the app update killed it at its last step and it was resumed from its saved transcript to finish its own handoff; gate 113 green with 478 smoke checks): the album's head as a catalog of seven asks on Maya and Jay's wedding, every tile drawn by the engine's own frames (the lane's recommendations: `tile` the engine playing as a thumb-mode crossfade loaded after first paint, with a stills crossfade and one framed still as the cheaper two, the engine's cost measured at about 40 KB gzipped of source and none for the stills option; `verbs` Watch alone, since the view carries Add yours and Make your own; `states` nothing under three items or with the switch off; `yours` the corner swapping to "yours is in it"; `door` the album's stills dimmed as shipped, the moving reel only where access is already full; `closed` the tile playing in place always; `hub` the labelled card as shipped, arguing against a picture among three text cards). Three defects the gate found were fixed in the lane (a same-picture collision under reduced motion, a set-state-in-effect lint error, a display-contents measurement bug). Calls his to overrule: the tile is a full-bleed horizontal reshape of the shipped poster card (2:1 at a phone, 21:9 at a laptop) because the slot above the album is a wide band; the wedding world for continuity with the other reel boards; the crossfade paces (about 1.1 s a still where the engine plays, 3.2 s for the no-engine option). Look at first: `tile`, since every other ask assumes its answer, then `hub`, whose recommendation argues against giving the flagship a picture. Nothing here wires production.

- **`reel-story` on the desk at `8012e393`** (2026-09-22, handed off `5b15b40c` by Sonnet on :3136 after the app update killed it mid-build and it was resumed from its saved transcript to finish its own handoff; gate 114 green with 486 smoke checks): the marketing story of the reel as a catalog of seven asks, every option the real copy in the real piece (the home's close band, the feature hub's reel door, the comparison matrix reading `MAX_REEL_SECONDS` live, the loop's numbered rail, the event pages' door) and the "real engine" claims drawn by `CanvasReelPlayer` over the shared album (the lane's recommendations: `thesis` "Every event has a reel.", the smallest true rewrite of the retired line; `arc` the live reel, the screen, then the cut, as three chapters replacing four sections built for the stored reel; `teaser` the demo album's live reel through the engine over the parked film and a still; `pricing` the rows renamed "Cut length" and "Cut watermark"; `steps` "Watch the reel grow" and "Make your cut" on both sides of the rail; `events` the live reel on the wall for each type, the demo door unchanged; `help` "The reel", the footer's own name today). Every recommendation carries its overrule line (cut-first is the stronger hook from a shared link; the film reads more cinematic as a hero; a footnote is more honest if the matrix should show only what is gated; a screen step names the host's one real lever; a guest's cut keeps the column personal; "The live reel" heads off confusing the reel with a cut). No asset needed (the parked film reused). Look at first: `arc`, the tallest and newest, then `teaser`'s engine option. Nothing here wires production; the copy the sweep lane ships comes from his verdicts here.

- **`reel-host` on the desk at `2ed05330`** (2026-09-22, handed off `baf8a6e0` by Sonnet on :3135 after the app update killed it mid-build and it was resumed from its saved transcript to finish its own handoff; gate 115 green with 493 smoke checks; the round's last board): the host's side of the reel as a catalog of six asks, every still from the shared bootstrap set and the mood swatches as gradients (the lane's recommendations: `style` a new Reel card in the settings sheet with the eight mood swatches; `switch` a third row beside "Show the guest list"; `screen` a fifth door in the hub's cards row; `pulse` the event card's own cover crossfading through the album, no words; `cut` one small sheet naming the storage cost before a host's cut lands; `review` a quiet line in the reel's own chrome linking to Review). One question it settled for itself and names: the brief's "beside the guest list switch" read as schema, not placement, so `switch` stayed a real three-way ask. A lab finding worth keeping for every later board: a lab frame's document never cascades a real height to a percentage `min-h-full`, so an absolutely positioned full-bleed child collapses to 0 px; fixed positioning for a standalone scene, explicit pixel heights from the screen table for a cropped composite. Look at first: `pulse`'s cover option, then `style`'s composite of the view's shortcut above the sheet's picker. Nothing here wires production. `[preview]`: the alias moves to this record, and the six reel boards and both harnesses meet his desk review there.

**Next.** Each handoff integrated as it lands and recorded here through `usher/kit/record.py`; the six boards on the desk
in leverage order directly below `media-viewer`; his desk review; then the wiring lanes, the expand migration first and the
drop after the red-team; the reel's system doc at the record.

## 2026-09-20 — The closing sitting: the desk's head answered (`fe056e62` onward)

**What Will did.** At 18:20 EDT, on the alias's `fe056e62`, he answered the desk's first five boards in one paste:
every open ask of `body-type`, `app-shape`, `guest-shape` and `app-vocabulary` (their round twos) and all six of the
new `guest-verify` board: 14 verdicts, seven confirming, five overruling, two `?` each carrying his own answer; every
note verbatim in rulings.md ("the closing sitting's first batch") with his summing-up ("we still need to find the best
shape across login, verification, upload credits, anonymous accounts ... Looking for best shape overall") and his
instruction to pause Moltbook. The plan, approved in plan mode after a second architect red-teamed the identity brief
(the round-one board's rate-limit wall was misread; two ideas demoted to recorded refusals; the host's never-refunding
meter made a decision), cut six lanes on the six seats: four wiring lanes that retire their boards, the desk pass
`overtaken-3`, and `guest-verify` round two on the identity shape whole. His four `guest-verify` rulings (`gate=after`,
`badge=mark`, `host-lens=badge`, `expiry=host`) are recorded and HELD, not wired, until round two rules ("May have to
relitigate"). The briefs are in the plan file and in each manifest.

**The record.** The five ledgers written (`guest-verify.json` created; 14 recorded, none echoed); the review sheet
rendered from the desk's captures (14 pictured) for the lanes and for Will; the kit's scripts honor an exported
scratchpad path and the heartbeat pauses Moltbook (`87ede4da`).

- **`guest-verify-r2` merged at `d68ef23f`** (2026-09-20, Opus; the board's round two, lab-only): the identity shape drawn whole on the shipped door, album, faces row, queue and settings sheet at 375 and 1440: `address` (none / private / public), `allowance` (handful / budget / open), `unproven` (shown-marked / shown-plain / held; `outage` answered inside it as a frame note), `collision` (offer / label / require; his cases 1 and 3 drawn beside it as settled fact), `gate-switch` (two / one / none); round one's four rulings HELD on the board's head and on every frame that rests on them; the rate-limit wall corrected to Supabase's current table (verify 360 an hour per IP with bursts of 30, so a room queues rather than walls; otp 360 an hour project-wide; the custom-SMTP emails cap ours to raise), documented defaults labelled as such, the configured numbers riding `call:numbers`. The Handoff's one-page recommendation is a THIRD shape, `none` + `handful` + `shown-marked` + `offer` + `one`, needing no new identity column (unproven is derivable from `guests.user_id is null`, since `guests.email` is verified-at-join): a display name typed at the door, ten photographs and no video per unproven session, the mark as the prompt aimed at the one person who can clear it, the same offer to every guest after the first upload, the switch keeping its view half; the migrations written only. Read by the Orchestrator against the invariants (RLS, verified-at-join, no oracle, the claim never re-stamps, deletion the only exit, the host's never-refunding meter): it holds. Gate 66 on the merged tree (Tests  3056 passed; smoke 443 checks, 0 failing; demo guest-verify 5 steps, 0 failing; log $S/gate1.log). Three carried calls his: `numbers`, `handful`, `vouch`.

- **`buttons-wiring` merged at `224049d6`** (2026-09-20, Sonnet; `body-type` round two wired whole, the board retired): every `Button` size's icon one Tailwind step over its own text (12/14, 14/16, 16/18), explicit on all eight sizes in `button.tsx`'s `cva` table so none falls back to the base `size-4` by accident (`icon-sm` had, silently, the exact mismatch he flagged); `sm`'s text onto the ladder as `text-xs`; the five `text-[15px]` overrides on the door's and the password gate's buttons moved to `size="cta"` (44 px, the door's own precedent) and the two `board` exceptions deleted from the ladder policy in the same commit; a contract test for the pairing (27 cases); `body-type` retired (registry, boards, touchpoints; a `RulingId` still, its ruling rewritten as shipped; the ledger deleted at the merge); `design-system.md`'s button line refined. Gate 67 on the merged tree (3079 tests; smoke 440 checks, 0 failing; demo toasts 3 of 5, `material` and `action` FROZEN, the harness's stacked-srcdoc blind spot verified by hand by the toasts lane, accepted on that standing evidence; log $S/gate2.log). His to overrule on the alias: `icon-sm` at 14 (the lane's re-derivation from the board's own measurement code; the plan had said 16), the `cta` icon at 18, the door's CTAs now uniformly 44 px with 16 px text (look at first: the guest door's three buttons and the password gate's two).

- **`controls-home-wiring` merged at `ddf4db47`** (2026-09-20, Sonnet; `app-vocabulary` round two wired whole, the board retired): one shared `ViewMenu` (`shared/view-menu.tsx`, radio groups as props on the shipped dropdown) holding Tile size, Sort and Filter behind one View button beside Add photos, Download and Select in the hub gallery's header; the Deleted lens moved inside Filter (All / Deleted); Sort's two orders drawn but DISABLED until the client holds the whole approved list (the brief's honesty line; a follow-up when it does); `tile-size-control.tsx` unmounted and kept for the lab; `app-vocabulary` retired (a `RulingId` still; the ledger deleted at the merge); `host-app.md`'s gallery lines refined; the guest album mounts the same menu under `guest-chrome-wiring`. Gate 68 on the merged tree (Tests  3079 passed; smoke 438 checks, 0 failing; demo guest-verify 5 steps, 0 failing; log $S/gate3.log). One help line stale (`your-event-page-explained.mdx:33`, the next `help-sync`). His to overrule on the alias, signed in: the trigger's word and icon, Filter without a photos / videos split, Sort disabled.

- **The toasts verdicts** (2026-09-20 20:05 EDT, build `c75734b`): five of five, every one the board's own recommendation, no notes (`where=top`, `material=card`, `life=persist`, `stack=expanded`, `action=always`); the paste verbatim in rulings.md ("the toasts board"); `toasts-wiring` (Sonnet, :3131) cut at this record; the board retires at its merge.

- **`overtaken-3` merged at `8c30faf6`** (2026-09-20, Opus; the desk's third judgment pass, for the closing sitting's first batch): twenty-nine asks reached by the fourteen verdicts (the map 62 to 72 keys), eight new judgments (six stands, two concedes), nineteen lines gaining a clause, and a THIRD grammar for the four held `guest-verify` rulings: the badge names the hold and his own clause and carries no judgment (two asks, `seed-avatar.look` and `first-event.first`). The heaviest reach is `guest-shape` round two (fifteen: `welcome=sheet` retires the centred float at a desk that four boards' options drew; `chrome=both` puts a dock at the foot of every guest album, which `toasts.where` and `help-center.from-product` waited on; `theirs=mark` puts a fourth mark on a tile, so glass's three-marks line has moved). The queue test's `first-event` pin (four badges, `asks` unreached) had kept two real badges out of two passes; lifted as the lane's granted exception, the count and the unreached ask now derived from the map, and the two badges are in. Gate 69 on the merged tree with `guest-chrome-wiring` (3102 tests; smoke 435 checks, 0 failing; demo guest-verify 5 of 5; the harness sight check sees; log $S/gate4.log). The hand merge: `docs/design/library.md` regenerated rather than merged.

- **`guest-chrome-wiring` merged at `fd42c759`** (2026-09-20, Opus; `guest-shape` round two wired whole, the board retired): the row on landing and a dock at the foot once it scrolls away (`guest/guest-action-dock.tsx` on the row's own sentinel: Add beside Invite as a `role="group"`, a gradient scrim, safe-area padding, `inert` while hidden; the floating Add pill's product mount gone, the file kept for three lab surfaces); the door's desk half on the responsive Sheet (a full-height right-edge panel with the album blurred beside it, the option's own posture: HIS TO OVERRULE, one line back to the centred dialog at 640 and up), its phone half vaul with the Sheet's height cap, the dismissability table and the step machine untouched; the one grid gained `mineIds`, `data-mine` on the tile box and a fourth mark in the play mark's glass recipe at the tile's top-left, whose tap filters the album to yours under "Showing yours · Show all" on the server-read own-uploads set Remove already gates; a Tailwind v4 landmine found in the browser and fixed (a transition named `transform` while v4's translate utilities set the standalone `translate` property, so the dock teleported); the board retired in the house convention (the `RulingId` and its row kept, `SandboxId` and `DESK_ORDER` gone; the ledger deleted at the merge); `guest-flow.md` refined. The guest album's View menu (tile size and Yours) waits for a one-line follow-up now that `ViewMenu` is on the tree. Gate 69 on the merged tree (3102 tests; smoke 435 checks, 0 failing; demo guest-verify 5 of 5; the harness sight check sees; log $S/gate4.log). The hand merge: `registry.ts`'s adjacent import deletions kept together, the generated files regenerated. Not exercised by the lane and left for the alias: the gated door's two steps (every test event is open) and the mark on a real own upload.

- **`home-states-wiring` merged at `5ea7415d`** (2026-09-20, Sonnet; `app-shape` round two wired whole, the board retired): the real collapse in `next-step-band.tsx` (three chips by tone, a "+N more" chip that expands in place with `aria-expanded`, "Show fewer", the chevron turning on `ease-emphasis`, reduced motion a plain swap), the band order from his `busy` note (the notices, then the storage line, then Your events, then Just arrived beneath the events; the page's header comment rewritten with his words), the empty and the one-event pages verified as already the ruled composition and left alone (`wizard`, `pulse`); the pulse's "Print the code" chip untouched (his to overrule); `app-shape` retired in the house convention (the `RulingId` and its row kept; the ledger deleted at the merge); `host-app.md`'s landing section refined. Gate 70 on the merged tree (3105 tests; smoke 432 checks, 0 failing; demo guest-verify 5 of 5; the sight check sees; log $S/gate5.log). The hand merge: the registration deletions kept together, the rows and the component notes as the union of both sides, the two retired ids dropped from `DESK_ORDER` where the union had re-added them (the typecheck caught it), the generated files regenerated. One help article stale, `your-dashboard-explained.mdx` (already stale since `home-wiring`'s pulse: it still describes the five-chip feed; the next `help-sync`).

- **`toasts-wiring` merged at `c92d653f`** (2026-09-20, Sonnet; the `toasts` board wired whole, the board retired): the Toaster at the top under the bar at both sizes (`top-center`, a 5rem offset that clears the marketing bar, the app shell's bar and the guest header, one number because one Toaster mounts globally), always expanded so a run of outcomes reads as full sentences newest on top, errors persisting until dismissed with a close control (sonner's `toast.error` patched once at module load, guarded against double-patching, the 65 call sites untouched; the other kinds on their clock), the popover card as today (verified a no-op), the trailing action as sonner's own `action` prop sanctioned for Undo and the refusals' Upgrade (no helper needed); a contract test on the real `toast` (one allow-list line in `floating-layer.test.ts` for it); `toasts` retired in the house convention and its two overtaken badges retired with it; `design-system.md`'s toast line refined. Gate 71 on the merged tree (3106 tests; smoke 429 checks, 0 failing; demo guest-verify 5 of 5; log $S/gate6.log). His to overrule on the alias: the 5rem offset (never measured on a signed-in host page), three visible toasts (sonner's default), the close control on a persistent error. The stylesheet released to the lane returns to the Orchestrator's owns at this record.

- **`guest-view-menu` merged at `86e41a28`** (2026-09-20, Sonnet; the follow-up of `guest-chrome-wiring`, no board): the shared `ViewMenu` in the guest album's control row beside Download all, Tile size (three steps, disabled below 640 with a "Wider screens" hint, the grid forcing two columns there) and Showing (Everyone's / Yours (n), present only once the guest owns something) on the same state as the own-tile mark's filter; the tile size resolved server-side from the shared cookie in the guest page and threaded down (one additive prop on `event-experience.tsx`, the lane's listed exception), the write through a server action mirroring the host's; fourteen tests; the first paint at the chosen size proven on the raw SSR HTML. Gate 72 on the merged tree (3117 tests; smoke 429 checks, 0 failing; demo guest-verify 5 of 5; log $S/gate7.log). Left for the alias: Yours after a real upload (R2's CORS allow-list excludes localhost). His to overrule: the group names and the hint's words.

- **The library index's duplicate key fixed at `ae20259b`** (2026-09-20, the Orchestrator; Will's report: `/design/library` threw React's "two children with the same key" for `src/components/shared`): the index grouped rows by consecutive runs of `dir` while the rules artifact lists a directory's files in several runs; `groupRowsByDir` (an insertion-ordered map, the artifact's order kept) merges every row of a directory into one group; three unit cases; verified in headless Chrome against the dev tree (188 rows, 38 groups, no console error; a control proves the collector sees a same-key error). The sibling `/design/library/components` still logs its own duplicate key (the ROADMAP's `admin-wiring` line, two Surfaces blocks), untouched.

- **The closing sitting's second batch** (2026-09-20 ~22:45 EDT, build `c75734b`; the paste verbatim in rulings.md): thirteen verdicts on the four drawn round twos and on `app-pricing` whole (eleven the boards' recommendations, two overrules: `phone=stack`, `carry=cards`), seven of them overriding badges earlier rulings had set (echoed in `_window.json`); six lanes cut at this record: `avatar-mesh-wiring`, `welcome-film-wiring`, `demo-frame-wiring` (Sonnet), `pricing-split-wiring`, `app-pricing-wiring`, `overtaken-4` (Opus); five boards retire at their merges. His word on the components gallery's duplicate key: taken the Orchestrator's way, after the cut.

- **The components gallery's duplicate key fixed at `6b4ecd46`** (2026-09-20, the Orchestrator; Will: "take the components gallery duplicate key the same way"): `familySections` grouped a family's entries by consecutive runs of `section` while lanes add entries at the head under headings that exist further down (the disjoint-hunk convention), so "Surfaces" drew as two blocks with one key and one DOM id; one shared first-appearance grouping (`gallery/group-by.ts`) now serves both the family pages and the library index (`groupRowsByDir` delegates to it), the merged block sits where its section first appears; three helper cases and a source pin on `familySections` (the runtime registry cannot load in a node test without the product's env, so the pin reads the source); verified in headless Chrome against the dev tree with and without the change (the duplicate key gone, zero exceptions; a pre-existing next/image `sizes` warning on the hero specimen remains, a ROADMAP line). The four gate steps green.

- **No push deploys any more (2026-09-20 late, the Orchestrator; Will: "we're wasting a ton on launch-prep, even for minor things ... stop deploying launch prep automatically, and intentionally deploy the review branch for each review round ... Or if there is a better system, implement that"):** Vercel's Hobby cap (100 deployment creations per rolling day, canceled ones included, both projects on the repository) filled at 20:03 EDT on the Orchestrator's own pushes (about forty to `launch-prep`, each creating two deployments the ignore step canceled at once, which the prune then deleted, so the dashboard showed almost none) and pinned the alias at `c75734b9`. `vercel.json` now turns `launch-prep` off beside `lp/*`, so a push creates nothing on either project; `usher/kit/alias-ensure.mjs` is the deployment: one per project by API at each `[preview]` record, both launch-prep aliases assigned by hand (about twenty creations a day); `scripts/prune-vercel-deployments.mjs` prunes both projects (the admin project had kept forty-three deployments, thirty-three of them canceled) and a canceled build no longer takes one of a branch's three keep slots. Not taken: a review branch fast-forwarded per round, because the allow-listed URL is the launch-prep alias itself; it is the fallback if an API creation for a branch whose git deployments are off is ever refused, which the first record after the window clears (about 20:00 EDT 2026-09-21) will show. Lanes lose nothing: they build and test on their own dev servers and never used the alias.

- **`overtaken-4` on the tree at `71e49638`** (2026-09-20 late, handed off `d354e44e`; the board commit `6451ad3a`, synced `fbe2030f`; gate 73 on the merged tree): the desk pass for the second batch, fourteen asks on the standing boards judged for the first time (every one a stand: a marketing-and-pricing batch rarely answers another board's option word for word; `press-page` carries its first badge ever), fifteen earlier lines gained a clause behind their judgment (`MAX_CLAUSES` to three), the seven overrides his answers made closed against their `_window.json` echoes (`seed-avatar.look` over `guest-verify`'s hold, spent by `look=mesh` without round two: the second way a hold ends, now in the file's contract; `app-pricing`'s six over five earlier rulings), the one hold left `first-event.first` (`gate=after`). Map totals 69 stands, 7 concedes, 1 held; seventy-seven entries on seventeen boards. Two calls his to overrule: the sheet cross-wire recorded, not resolved (`object=sheet` read as the one responsive Sheet; the badge says a centred surface is "alive again at a desk"), and "Convert, not block" generalised to a guest's allowance cap and the download limit.

- **`pricing-split-wiring` on the tree at `9ab4b243`** (2026-09-21, handed off `4fdf77bf`; the board commit `d3d796b7`, synced `ef893383`; gate 74 on the merged tree; `pricing-page` RETIRED, its ledger deleted): `fit=split` wired whole: the album wall became the split configurator, one bordered panel of two planes (the storage slider on `STOP_GB`, the video switch and the once-or-again fork recessed on `bg-muted`; one elevated photographed plan card on the page's own white as the live result, its deck of prints fanning as the slider climbs, one at 1 GB and four at 2 TB, so the wall's "watch your album fill up" survived the wall); `calculator.tsx` became `configurator.tsx` (`STOP_GB` still exported), `PhotoStack` joined `StatRow` as an export of the pair. His chapter note: the configurator closes the paper chapter directly under the pair and the Event Pass ticket, and `UnlockGrid` opens the dark one as its overview, then the matrix, then the FAQ (his round-one "tiles above Find your plan size" superseded, as he said); both halves pinned in `pricing-page.test.ts` and a new five-clause contract. `phone=stack` changed nothing at 375. `touchpoints.ts`'s 2026-08-27 `pricing-calculator` row names the configurator as the wall's successor (called out, not taken silently). Six calls his to overrule on the alias: the result card's photograph (a fourth stack of prints can read as a fourth plan), the recess at `bg-muted` (the only real step paper offers; deeper is a token change), the figure at `text-prose`, the panel at `max-w-5xl`, the new subhead (the board's was false at 375 where the halves stack), the live region as the verdict alone. Verified locally at 1440 and 375 with real CDP keyboard drive; the alias pass is the Orchestrator's once the window clears.

- **`app-pricing-wiring` on the tree at `a3ae7e88`** (2026-09-21, handed off `fe0efb14`; synced `5a636358`; gate 75 on the merged tree; `app-pricing` RETIRED, its ledger deleted): all eight rulings wired on the shipped chrome. Pricing opens INSIDE the app: `src/components/app/pricing/`'s `PricingSheet` on the one responsive Sheet, led by its trigger (a locked feature names itself; `room` resolves through the marketing calculator's own `smallestProFor`; a subscriber is told she subscribes and handed the portal), Free beside one Pro size with three benefit lines, the pass on one line, a quiet foot to `/pricing` in a new tab; `LockChip` replaced the four sentences that worded one rule four ways, a pressable button with a tooltip (his "Convert, not block"); the user menu's Plan and storage row above Account carries the plan's name and opens the account page's Plan card (Upgrade and the pass line there; NO dedicated Billing page, the honest list of what one would need is in the retired manifest's Handoff: a pass ledger, a per-event storage breakdown, cap alerts; revisit when the ledger exists, and call it "Plan and billing"); `WelcomeToPro` retires `upgraded-toast.tsx`, heals the webhook race with a bounded re-read; `return-path.ts` is an exact-shape same-origin allow-list on `success_url`, so Checkout returns to the control that refused you (a purchase started elsewhere returns to the dashboard with the same modal). The eleven `/pricing` doors in the host app are zero. Two things worth his eye first: the Free card is dropped for an Event Pass holder (a pass holder cannot move to Free), and the dashboard's two banner doors were converted (they are the `room` trigger). Calls his to overrule: the three benefit lines; "Plan and storage" with a `CreditCard` glyph; the modal's two doors; the chip's tooltip words in `pricing/triggers.ts`; the chip's face saying Pro. Verified by 60 contract assertions and the eye on a bare stage; the checkout round trip (Stripe TEST) and the webhook race are Will's on the alias, signed in. Eleven help articles are stale (a ROADMAP line for a `help-sync` lane).

- **`welcome-film-wiring` on the tree at `d834d67f`** (2026-09-21, handed off `bf19d0fb`; synced `cf363ee2`; gate 76 on the merged tree; `app-door` RETIRED, its ledger deleted): `tour=film` wired: `/welcome` is five real screens, the required name step untouched, three of the marketing site's own how-it-works pictures (`StepPicture` for create, share and fill, quoted rather than redrawn) breathing under an overlapping copy plate on a slow 16 s linear drift (`welcome-flow.css`, reduced motion gated, a bible-12 exception named in its comment), closing on `ReelPicture` into the unchanged primary-and-skippable pair; a contract test pins the screens and exits; `host-app.md`'s welcome section refined. Calls his to overrule: the three pictures and the 16 s (as drawn and confirmed); the dots-and-Skip header kept across the tour as the original tutorial's chrome (a way out before the fourth screen); the closing line "Create your first event and share the code." One help article stale (`create-your-first-event.mdx`, "a three-screen tour": the help-sync ROADMAP line). The welcome tour is Will's on the alias signed in as an unwelcomed account.

- **`avatar-mesh-wiring` on the tree at `ca8d656d`** (2026-09-21, handed off `d4e1325b`; the board commit `af9af05c`, synced `f9d17a02`; gate 77 on the merged tree; `seed-avatar` RETIRED, its ledger deleted): `look=mesh` wired: the mesh register in `src/lib/avatar/gradient.ts` (one identity hue at four diffused, blended tonal depths, hashvatar's own register read from its source and credited), `ui/avatar.tsx`'s one `"diagonal"` call now `"mesh"` plus the `backgroundBlendMode` the register needs (kept for fidelity to the look he approved; his to overrule), the board's centre-pixel compositor beside the generator as `measure.ts` with its contract test, and `gradient.test.ts` holding the shipped look to 4.5:1 at the disc's true centre across a thousand real UUIDs (the `avatar-look` ROADMAP line closed); `auth-accounts.md`'s avatar line refined. His to overrule: the diffusion's strength at 24 px (one `meshDepths()` formula at every size). Look first at any photo-less account's avatar on the alias (`/u/willg`).

- **`demo-frame-wiring` on the tree at `38a04201`** (2026-09-21, handed off `1603a87d`; synced three times, last `d1da0f17`; gate 78 on the merged tree; `demo-event` RETIRED, its ledger deleted; the second batch whole): `door=frame` wired: one `DemoFrame` (a photograph in a plain mat, the live code tucked into its corner) replaced the hero's bare QR plate, the footer's four-photo fan, the feature page's bare text line and the mega panel's empty featured pane. His note answered by measurement against the shipped hero, not the board's mock: the frame stands 218 by 258 at 1440 over corridor tiles of 48 to 253 px (the busiest 60 to 204) and 162 by 191 at 375 over 24 to 137, lifted by the mat's own shadow, the headline clearance positive down to a 680 px window. Calls his to overrule: the corner code at 92, 72 and 108 px (smaller than the old bare QR's 144 and 128, a photograph-forward object); the stream neither pauses nor dims under it (`hero-stream.ts` untouched, no prop existed); `DemoTicket` wraps `DemoFrame` for the Library specimen and the site-chrome sandbox; no party name on the object. `marketing-content.md`'s demo line refined after syncing past `pricing-split-wiring` (its one named exception). No help article stale, no asset asked. Look first at the home hero at 1440 and 375 on the alias.

- **The closing sitting's third batch transcribed (2026-09-21 ~01:40 EDT): `first-event` and `guest-upload` round one ruled whole**, sixteen verdicts, fifteen of them on asks an earlier ruling had badged (the overrides echoed to `_window.json`; the last held badge, `first-event.first`, spent by his direct `first=live`). Cut at the record as three lanes on three seats: `first-event-wiring` (Opus, :3131: one big name field, the style step redesigned, the door at the cap before the form, the print stock in its own route group, the beat once, the share sheet redesigned and the legacy dialog retired, the launch list, the live hub by the doorbell and a host fingerprint route; `first-event` retires), `guest-upload-wiring` (Opus, :3132: the intent sheet with a review step, the stack tile on a darker scrim, the sweep on one's own landing with the arrival grammar unified in the masonry, the waiting tile, the end-of-run failure sheet, the terms line, every sentence at the reading rung; `guest-upload` retires) and `overtaken-5` (Opus, :3133: the desk pass, merging first). His notes made structural where they add to the option (the review step; the print stock, its gallery of designs banked); the redesigns inside the wiring; the identity board untouched.

- **`overtaken-5` on the tree at `7e4d7212`** (2026-09-21, handed off `12d91175`; the board commit `ba309d04`, synced `59bc51f0`; gate 79 on the merged tree; merged FIRST, before the two retirements): the desk pass for the third batch: seven asks on the standing boards badged for the first time (`guest-verify.gate-switch`, `media-viewer.link`, `export-flow.hollow` and `.phone`, `contact-page.topic` and `.urgency`, `press-page.the-sheet`), thirty-one earlier judgments gained a clause, the fifteen badges the two retiring boards held removed behind comment blocks (77 entries to 69), the last held badge (`first-event.first`, `gate=after`) spent by his direct answer so the map holds no hold and the grammar's test proves the shape on that hold's note; `MAX_CLAUSES` stays 3 (the cap is a measurement, its comment says so now). The desk's join test (`_desk/queue.test.ts`) proved the join on `first-event` by name and now derives the board it proves on, which both wiring lanes inherit by syncing past this merge. Lint's baseline is 10 warnings, not 8 (stale by two, none in this lane's files; the spawn brief's number is refreshed at the next cut).

- **`first-event-wiring` on the tree at `ef42541d`** (2026-09-21, handed off `0af454d6`; synced past `overtaken-5` at `bb903919`; gate 80 on the merged tree; `first-event` RETIRED, its ledger deleted): the eight rulings wired. One borderless name field at the section step, the note and the date left for the event (`asks=one`); the picker redrawn as four container-query swatches sized by CSS, 160 px codes at the wizard's width and 4-up wide (`style=step`); the refusal before the form from a prop the island snapshots at mount, so the post-create refresh can never take the beat back, and the dashboard's disabled button a live link again (`limit=door`); step 3 the beat, reachable only by Create (`landing=beat`); a print route group `src/app/(print)/` with its own auth gate, three pieces in millimetres through the zero-JS `FooterQr` under one print hook and no `@page` (`venue=sheet`); the share sheet redrawn at 375 with the code at the sheet's width and one row of four verbs, the legacy dialog deleted, the card chip a link to `?room=share` (`hand=same`); a launch list from the event's own nulls (`empty=list`); the doorbell plus `/api/events/<id>/live`, `router.refresh()` only on a ping or a changed validator, never a timer (`first=live`). Two calls his to overrule first: the printed code is the classic shape whatever preset the event carries (zero-JS paper), and the table card is 62 by 84 mm, nine to a sheet, because nine A7 cards never fitted one (the board's caption said they did). One exception of four lines in `gallery-demos.tsx`. Localhost cannot sign in, so the signed-in pass is Will's on the alias: the door at the cap, the big name and the picker, the beat, the print sheet in a Letter and an A4 preview (the arithmetic is tested; the type's scale on paper is a judgement), the launch list, the live hub. Signed out: print answers 307 to `/login`, the live route 401.

- **`guest-upload-wiring` on the tree at `4217f1d9`** (2026-09-21, handed off `03c900a8`; the board commit `321813b2`, synced past `overtaken-5`; gate 81 on the merged tree; `guest-upload` RETIRED, its ledger deleted; the third batch whole): the eight rulings wired. Every Add opens the intent sheet on the responsive Sheet, two rows over two hidden inputs inside `SheetContent` (the camera takes one photograph, the album takes both kinds), each clicked inside the gesture (`tap=sheet`); the picker returns into the same sheet as a review step, an accidental pick one tap from gone, an undrawable file a named stand-in with its size, the terms line stating the product's limits with `capBytes` as the seam for the host's own number (`warning=both`); one stack tile per pick led by the file in the air, its count and bar on one pane measured 4.78:1 for white over a pure-white photograph, the board's black/45 having read 3.35:1 (`batch=one`, `sending=strip`); the waiting tile (`held=tile`); the end-of-run failure sheet with the server's own sentence per file and Retry all, exercised end to end on a real event (`failed=sheet`); one arrival grammar from `lib/shared/arrival.ts` and `shared/arrival.css`, the hold exclusive so a twelve-file batch never lights two tiles at once, the thing the shimmer was banked to avoid (`landing=sweep`); every sentence at the reading rung (`words=read`). Both upload toasts and the green check retired; one owner per blob URL after StrictMode's remount painted revoked previews. Not exercised locally: the waiting tile (a hold-for-approval event) and the glow (a second device): the alias pass. The host hub keeps its own arrival hold from `first-event-wiring`; folding it onto the shared hook is one ROADMAP line.

- **`third-batch-fixes` on the tree at `230e6aa8`** (2026-09-21, handed off `298e9c94`; gate 82 on the merged tree; no board): the alias red-team's two defects and one polish item in the third batch's wiring fixed in place. A dismissed upload failure leaves the queue for good (`dismiss(ids)` in `use-upload-queue.ts`; Not now and the sheet's close call it), so the end-of-run sheet never re-opens on a later run with an old failure; the print stock's link line wraps inside its own face (`wrap-anywhere` bound to the face's width, the font one step down past 70 characters) instead of running into the next card; the intent sheet keeps its review step mounted through its own close instead of flashing the two intake rows on Send. `guest-flow.md` carries the dismiss fact. Deferred: the guest header's contributor count under-counts a guest's own first upload until reload (a server signal, not a client heuristic).

- **The identity reshape cut** (2026-09-21, build `5e210ef`; one verdict, `guest-verify` `address=none`, and his note; the plan reviewed twice with fresh context at his ask, approved ~14:45 EDT): anonymity leaves the product on his shape. The host's switch becomes Require verified emails (on by default: on, a guest confirms an email before the full album and any upload; off, a guest types a display name at the door and uploads under it with a small unverified mark), the capture flow after a name-only guest's first upload (keep the photos and the event on a profile, then follow the host and the guests) is wired as the working version with a board cut on his word for its refinement, unverified names are listed on the guest list with the mark, no cap, `gate=after` superseded, `guest-verify` retires at the wiring. Six lanes in three waves, the schema first: an expand migration production survives (`require_verified_email` beside the old flag under a sync trigger, `guests.display_name` and `verified_at`, the forensics name), applied by the Orchestrator before the wiring is cut.

- **The overtaken audit cut** (2026-09-21; Will's cleanup of the badged questions, his criteria verbatim in rulings.md): the sixty-six badged questions on the fourteen standing boards read one by one against the rulings that reached them (six answered, forty-four touched, sixteen unreached; none with zero value left): no question removed whole, every one reshaped with the rulings and the identity reshape folded into its context, fourteen dead options dropped, four new concepts added, every badge retired into its question. Four lanes, one board folder each; the map stays empty as the mechanism for a future overlap; a burn cycle's boards never overlap in what they ask (the program line).

- **`verified-email-migration` on the tree at `34736405`** (2026-09-21, handed off `8d6e8bd4`; the migration commit `bcd57361`; gate 83 on the merged tree; wave 0 of the identity reshape, no board): the expand migration applied to the project by the Orchestrator at the handoff, the rolled-back contract check green on every assertion (the trigger both ways and the contradictory write, the refusals, the nulled name, the rename, the post-flip refusal that production maps to uploads_closed, both flags from the door RPC, every grant, the backfill), the advisor sets as expected (five anon reads, the two new functions in neither list), the types regenerated onto the tree, production's demo album and home page still serving; the lab's gallery fixture gained the new column at the merge (the regenerated row type requires it: the one red the merge chain caught). The lane pre-flighted the migration on a throwaway Postgres 17 cluster and caught a table-level revoke that would have cascaded onto every column grant and taken the host app down at the apply; removed, and recorded in `database-security.md` beside its mirror gotcha. Two findings for wave 1: identity keys on `guests.verified_at`, never `user_id` alone (an unconfirmed session carries a uid); the DB accepts a nameless mint so production keeps working, and the name requirement is the route's.

- **`reshape-studio-export` on the tree at `17436828`** (2026-09-21, handed off `b4064be4`; the board commit `265ca025`; gate 84 on the merged tree; the overtaken audit's first lane home): every one of the sixteen badged questions on `reel-studio` and `export-flow` reshaped with its rulings in its context, none removed; `reel-studio.door` redrawn on the shipped hub (what the Reel card shows: a labelled card, or the reel's own frame with Edit reel at its corner); `reel-studio.wait` gained the reel's frame stacking and counting down, from the shipped stack tile; `export-flow` dropped five options a ruling forbids (`chips.three`, `stuck.forever`, `hollow.silence`, `cap.bite`, `object.link`) and is drawn on the one responsive sheet the guest's dialogs are ruled onto; two recommendations moved (`styles` to the wall, since the side panel answered the rail's case at a desk; `object` to the zip); the sixteen badges left the map, and the map test's floor became a proportion so every later deletion passes. His eye: the door's redraw, `face` over `card`, `styles=wall`, the new `stack`, `object=zip`, and the export board drawn on the sheet while the shipped dialog is still centred (a wiring line, deferred).

- **`reshape-viewer-curation` on the tree at `cde38921`** (2026-09-21, handed off `bc432749`; the board commit `9ff57acf`; gate 85 on the merged tree; the overtaken audit's second lane home): all fourteen badged questions on `media-viewer` and `host-curation` reshaped in place, each ruling written into its question as the ground the answer stands on, none removed; `media-viewer.opening` redrawn on the ruled ground (the production blur utility, the dark room renamed `fade`); `who` dropped `none` (anonymity left the product) and gained `face`, a seeded-avatar credit with the unproven mark on its corner; `host-curation.peek`, the audit's one concede, reopened at a desk where his narrowing of the lightbox rule left it; the fourteen badges left the map with the two handles they orphaned; the map test de-named the ask it pinned. His eye: `opening`'s three options on the ruled ground, `face`, the desk's peek.

- **`verified-email-lab` on the tree at `1869dbec`** (2026-09-21, handed off `5e5631db`; the board commit `452f1713`; gate 86 on the merged tree; `guest-verify` RETIRED, its ledger deleted): the identity board retired on his four answers (every ask ruled): out of the registry, the boards list and the desk's order, its folder deleted, its RULINGS row rewritten as shipped with its `lives` list; its three entries left the map with it; the HELD note gained its third ending, a hold superseded by his own next shape, and no badge was added for the reshape's reach (the audit folds it into the questions). The map's own test now survives an empty map (censuses derived from the map, both grammars proved on constructed notes); three unused batch markers left the map; the desk's join test guarded against an empty map at this merge.

- **`verified-email-server` on the tree at `6fae1257`** (2026-09-21, handed off `37cab894`; the board commit `912880e4`; gate 87 on the merged tree; wave 1 of the identity reshape, no board): the join takes a display name (`POST /api/guests {qr_token, display_name?}`, 422 `verification_required` | `name_required` | `name_invalid`, the route owning the name requirement and the profanity gate); the rename door `POST /api/guests/name` over `set_guest_display_name` with its own `rename` limiter kind; presign and complete refuse 403 after a mid-event flip with a captured warning, and the mutation mapping splits the deliberately ambiguous refusal back out above the branch production matches; the one precedence rule keyed on `guests.verified_at` (`isVerified` through both grid builders and the gallery ETag, `g1` to `g2`); the guest list's union behind `includeUnverified`, `getHostCard`, the forensics name captured, `require_verified_email` on the guest event, the access gate and event creation (default true). `email_required` gave way to `verification_required` in the error taxonomy (a forced cross-lane line, both callers this lane's). Red-teamed live on its port: every refusal by hand, an injected email stripped, a foreign token renaming nothing, a presigned URL that outlived a flip refused at complete with no row written, a rename never rewriting the forensic name.

- **`reshape-admin-help-emails` on the tree at `199435be`** (2026-09-21, handed off `a4174070`; the board commit `718a47fa`; gate 88 on the merged tree; the overtaken audit's third lane home): all seventeen badged questions on `admin-triage`, `help-center` and `emails` reshaped with their rulings in their context, none removed; `admin-triage.reason` gained `chrono`, a new concept beyond the audit's list; `emails.moments` gained an `identity` option (the moments the identity reshape's capture flow implies) and recommends it; `admin-triage.notice`'s recommendation moved from silence to a line to the host, since both rulings it folds in argue against a second silent gap; `idiom` and `escalate` kept three options each; the seventeen badges left the map. His eye: the notice flip, `chrono`, `identity`.

- **`verified-email-guest` on the tree at `f11c416a`** (2026-09-21, handed off `f1772647`; the board commit `efb874f0`; gate 89 on the merged tree; wave 1 of the identity reshape, no board): the door's name step (`guest-name-step.tsx`, an imperative second step through the entry shell at the first Add on a names-mode event, free to dismiss; `join.ts` over the route with its 422s translated; the name stored beside its session), the credit's unverified mark (`unverified-mark.tsx` in the own-mark's material, carrying its own way out on one's own upload; "Anonymous" gone, "A guest" for the rows from before; the explainer retired in place since the Library imports it), the capture flow (the offer card counting what landed, a pending flag consumed once on the next mount by the slot's owner on every sign-in path, the claim awaited before the save, the typed name reaching a nameless profile through the profile's own write, the follow moment once with the handle claim folded in), the guest header's third state with a sign-in wear, the guest list's unverified chips and a Follow on handled ones, the reworded gate line and teaser pill, and the mid-run 403 (a silent re-join when confirmed; otherwise the session dropped and the run refused once on the failure sheet). Two seams read the server lane's names through one narrow cast each, correct on both sides of that merge (a collapse on the ROADMAP). The capture flow is not verifiable on localhost (the R2 PUT fails CORS there): the alias proves it.

- **`verified-email-host-copy` on the tree at `b1f34fc4`** (2026-09-21, handed off `f7fb7c30`; gate 90 on the merged tree; wave 1 of the identity reshape, no board): the host's switch is Require verified emails with no inversion, asking on OFF (its copy set to the plan's approved sentences at the record, since the brief pointed at copy the manifest did not carry: "Stop requiring verified emails?", "Use names only" / "Keep verified emails"; the Library's ConfirmSwitch demo re-worded with it); `guestExperienceSummary`'s eight sentences retold; the Guests room opted into the unverified union (`includeUnverified`, split before the avatars); the guest-list settings copy; the reel's null bucket renamed legacy; fourteen marketing surfaces (the feature pages' "Anonymous" fixtures a named, marked guest, Theo), `marketing-voice.ts`'s rule NEVER WRITE 'ANONYMOUS', ten blog posts and both AUTHORING guides, seventeen help articles (`require-accounts-to-upload-explained` renamed `require-verified-emails-explained` and rewritten whole; `help-redirects.ts`, its spread in `next.config.ts`, a retired-slug test), Terms 1.4 and Privacy 1.3 with three reworded passages each, PRD and PRICING. The lane merged the server and guest lanes early and caught two cross-lane gaps before the tree did (the switch's `boolean | undefined`, the page's dead branch); four help descriptions trimmed under the 200 cap; the sweep accounted for every remaining "anonymous" by lane or by sense.

- **`reshape-marketing-boards` on the tree at `508e1098`** (2026-09-21, handed off `2622c50c`; the board commit `eab11e0a`; gate 91 on the merged tree, its one red test (the previous record's copy patch against the help article's quoted labels) repaired at the record with typecheck, lint and the whole suite re-run green; the overtaken audit's fourth and last lane): all nineteen badged questions on `site-chrome`, `profile-page`, `privacy-hero`, `album-motion`, `loose-ends`, `contact-page` and `press-page` reshaped with their rulings in their context, none removed whole; two redraws (site-chrome's footer register onto the shipped framed photograph; album-motion's three trips against the product's ruled arrival); two new concepts built from the product's real arrival sweep (`privacy-hero.concept`'s `sweep`, now recommended over `access`; `loose-ends.everywhere-pill`'s `sweep`, replacing the dropped desk-only `hover`); three recommendations flipped onto shipped objects (`profile-page.quick-look` from `adaptive` to `sheet`; `album-motion.fall` from `glide` to `cascade`, `glide` still shipping; `privacy-hero.concept`); `press-page.the-sheet`'s `brand-in-use` gained the app's printed stock at its real proportions; the rest gained their badge's precedent in context. With this lane the `OVERTAKEN` map is empty: the audit judged sixty-six badges across fourteen boards, removed none whole, dropped fourteen dead options and added four concepts; the map's sixteen dead per-batch handles were deleted at the record, on the file's own rule that a handle leaves with the last entry that spreads it (the mechanism stands until Will says it retires).

- **`guest-capture` on the tree at `5e93f714`** (2026-09-21, drawn and handed off in one commit `b70ddc81`; gate 92 on the merged tree; wave 2 of the identity reshape, the one new board of the sitting, on his word "I'd like to get this in the lab for refinement"): the capture flow's refinement catalog drawn on the SHIPPED components (the offer card, the follow moment, the guest's own menu, the profile a guest lands on), lab-only, five decisions over one guest, Priya (media-viewer's own fixture world): the moment the offer first reaches her (recommends `first`, as shipped), the offer's shape (recommends `inline`, a line under her own tile, over the shipped card), the follow surface (recommends `list`, folded into the Guests section, one surface instead of two), the landing (recommends `album`, as shipped, never a redirect) and what the typed name becomes (recommends `together`, name and handle settled in one step); phone first at 375 with 1440 on the knob; the sheet-step option a quoted reproduction of the bottom sheet, never a mounted one, and the list option's Follow a local control, never the real button; registered at the desk's head by the lane and moved by the Orchestrator to its leverage place after `media-viewer` at the record (the capture flow lives under the album's shape); its ledger opens at its first review. The desk stands at fifteen.

- **The identity reshape's red-team on the alias at `c7015a26`** (2026-09-21, 18:23 to 19:05 EDT; a new Orchestrator resumed the walk the previous session had begun at 18:05 and died in at 18:12, just after flipping the switch ON with a file queued; the pane signed out at 375 and 1440 on `Gallery width (disposable)` in both modes, Chrome signed in as the host): verified mode passed whole (the welcome then the gate's account step, "Just browsing" to the nine-tile teaser, the CTA reopening the gate, the third-state guest menu on a stale name-only session, the host's own event never gating, the Guests room's unverified chips, the hub lightbox's mark with the host's extra sentence, the switch's copy and its OFF dialog on the plan's sentences, the summary line both ways, the trigger keeping the twin flag opposite); names mode passed whole (the name step at the first Add, a dismiss minting nothing, a reserved name refused in place, the join, the intent sheet, three uploads landing with the own-tile mark, the pill's name and mark with the own popover's Confirm your email, the legacy rows as "A guest", the offer card once with Maybe later held across later landings, the four-item menu, Change name reaching the row and the credit on reload with the forensics name untouched, a nameless legacy session asked for a name, the mid-run flip dropping the session and gating the next act, `/demo` asking no name); the help slug's 308 and Privacy 1.3 and Terms 1.4 with their sentences; Ghost check verified and empty. Three DEFECTS and three polish items, cut as `identity-fixes` (Sonnet): the mid-run flip's failure sheet lives 503 ms before `router.refresh()`'s `key={access}` remount unmounts it (measured); a nameless legacy session's name step re-mints a row through the join instead of naming its row through the rename door; `captureWarning` never reaches Sentry from Vercel (three presign 403s in the alias's logs and no event; no warning-level event from preview or production in thirty days); the teaser's three numbers (9 loaded, 44 photos, 50 waiting) on one screen; a rename reaching the loaded credits only on the next poll; "Keep these photos" for one photo. For Will: the hub's Guests card reads 0 contributors beside an album "from 4 guests" and a Guests room of three named unverified guests; one person on one device is listed twice after the flip dropped their session; the lanes' localhost runs put four development issues on the Sentry board.

- **The door round's wave 0 on the tree** (2026-09-21, about 20:30 EDT; the schema alone, written and applied by the Orchestrator, bending PROGRAM.md's letter for a one-column, two-function migration on the identity reshape's exact pattern): `events.require_upload_to_view` (off by default, the bare additive column grant to the host), `get_event_by_qr_token` recreated with the switch beside `require_verified_email` and re-granted, and `get_upload_gate(p_event_id, p_session_token, p_user_id)` (service role only: has this session or account ever completed an upload here, and is the album full by the presign's own two caps). Dry-run in a rolled-back transaction first, then applied (`20260922003000_require_upload_to_view.sql`), the eight-arm rolled-back check green to its deliberate raise (the default, both column privileges, the door RPC mirroring a flip and keeping its grant, an unknown token, a nameless mint, a completed upload counting through hide and removal, a claimed row counting through the account and not the token, a one-byte cap reading full and closed uploads skipping it, a missing event reading gone, the grants), the advisors unchanged (the anon set still five, the new function in neither list), the types regenerated, the guards test extended, the guest event type and the lab fixture given the field, the ROADMAP's contract-migration line told to keep the column. Production serves throughout: nothing reads the column until wave 1 merges.

- **`identity-fixes` on the tree** (2026-09-21, merged `c1199470` at 21:08 EDT, gate 93 green with 418 smoke checks; handed off `5fd5fbe7` by Sonnet on :3131 after a clean sync-merge over wave 0): the identity red-team's three defects and three polish items fixed in place, nothing reopened, no board. The mid-run flip's failure sheet now outlives the refresh (the refresh waits for the sheet to close; a join-time refusal still refreshes at once); a nameless session with a held token renames its own row (the rename first, the join only on a dead token: `invalid_session` and `unauthorized` now recognised in `join.ts`); `captureError` and `captureWarning` schedule a server flush through `next/server`'s `after()` behind a client guard, with a bare flush outside a request scope (proven on :3131 against a bad-signature Stripe webhook and a clean build; the alias proof is the red-team's); the teaser's header, CTA and gate count one `approvedTotal` under the header's noun; a rename patches this device's own tiles locally and refreshes the page for the Guests list; the offer card's heading reads "Keep this photo" for one. One exception outside its owns, declared: a `component-notes.ts` entry for `sentry.ts` (the file's first contract test), plus `design:rules`'s own regeneration. Calls his to overrule: the refresh waiting for the sheet rather than the sheet surviving the remount; the CTA and the gate counting videos with the photos under the header's unconditional "photos & videos" noun; the `router.refresh()` on a rename (the Guests list is a server-baked node the lane could not reach locally); "Keep this photo". The save-an-event help article's quoted heading is one word stale (`Keep these photos` now varies by count); `door-steps` owns that article and carries it.

- **`identity-fixes` red-teamed on the alias at `8d83ec75`** (2026-09-21, 21:15 to 21:27 EDT; the pane at 375 signed out on `Gallery width (disposable)`, the flip done by SQL on the disposable row, a MutationObserver in the page timing the sheets): six of six hold. The failure sheet after a mid-run flip mounted 557 ms after Send and stood until "Not now" thirty-one seconds later, then the deferred refresh landed on the teaser (it had lived 503 ms); the legacy nameless row `ab7cc94d` took the name typed at the Add door onto itself, its token and nineteen photos intact and the event still at seven rows (it used to mint an eighth); `captureWarning` reached Sentry from Vercel for the first time: one `upload_refused_unverified POST /api/r2/presign-upload` at 01:24:30Z, environment `vercel-preview`, release `8d83ec75` (no warning-level event had ever arrived from Vercel); the teaser's three numbers agree (the count line, the CTA and the gate's "are waiting" line all say 54 photos & videos); a rename reached the header chip, the lightbox pill and the server-rendered Guests list within two seconds (the pill and the list used to wait for a reload); the offer card's heading read "Keep this photo" for one upload. One cosmetic nit, handed to `door-steps` which owns the file: on "Not now" the failure sheet's list clears 33 ms before the sheet leaves the DOM, so "0 files did not go / Retry all" shows during the 200 ms exit; the content should freeze until the exit ends. The event rests in names mode with 54 media and seven guest rows; the pane's keyboard cannot edit a field (End, Backspace and select-all all failed; the value setter with an input event is the way), a lesson for testing-verification.md.

- **`upload-gate-host` on the tree** (2026-09-21, merged `95f8008f` at 21:52 EDT, gate 94 green with 419 smoke checks; handed off `48725d49` by Sonnet on :3133 after a clean sync over the red-team records; the door round's first production merge): the host's side of the door ruling. A fourth switch on the Guest uploads card, Require an upload to view, off by default and free on every tier, the first switch in the card to confirm on the ON edge ("Ask for a photo before the album?" / "Ask for a photo" / "Leave it open"), with a hint that it has no effect while uploads are closed; `require_upload_to_view` through the zod schema, the mutation's insert and patch lists and the form's defaults; the guest-experience summary composing its ON sentences fresh ("Guests give a name and add a photo, then see everything.", the verified and password variants likewise) with the eight OFF sentences pinned unchanged; a new help article `require-an-upload-to-view-explained` on the verified-emails article's spine, a bullet in the settings article, a section of its own in who-can-see-your-event; one line each in host-app.md, the PRD (its stale "fast-follow" note corrected to shipped) and PRICING. No exception outside its owns. Calls his to overrule: the password-variant sentences ("Guests enter the password, then give a name and add a photo, then see everything."); the new article's body; the who-can-see article's third heading rather than a fold; the PRD's edit target. Deferred: the new article sits at the end of its category (`order: 7`) because bumping its four siblings was outside the lane's owns. The signed-in toggle-and-save walkthrough was blocked on localhost by the sign-in boundary and is the Orchestrator's on the alias.

- **`upload-gate-host` walked on the alias at `222b4b8a`** (2026-09-21, 21:58 to 22:03 EDT; Chrome as the host on `Gallery width (disposable)`, the pane signed out for the help): the fourth switch on the Guest uploads card asks on the ON edge with the plan's copy ("Ask for a photo before the album?", the description naming the preview photos and the fail-open, "Leave it open" focused first, "Ask for a photo"), stays OFF until confirmed, and once confirmed the summary composes "Guests give a name and add a photo, then see everything."; "Settings saved." and the column read true on the database and ON after a reload; the OFF edge asks nothing, the summary returns to the plain sentence, the save reads false; with uploads toggled closed the hint "Has no effect while uploads are closed." appears under the switch and the summary reads "Uploads are closed.", both gone when uploads reopen, the form left clean and unsaved. The new help article renders at its slug with the Off, On and Which-to-choose spine and its cross-links, the who-can-see article's new section and the settings article's bullet read right, no em-dash in any of the three. The event rests with the switch OFF.

- **`door-steps` on the tree** (2026-09-21, merged `cc9e7da8` at 22:51 EDT, gate 95 green with 419 smoke checks; handed off `ce3ce0d0` by Opus on :3132 after four API drops survived by message and a clean sync past the host lane; the door round's second production merge): the guest door became one held sheet with no exit on Will's ruling: the welcome, the password when there is one, the name, the email held until confirmed when the host requires it, the first upload asked inside the sheet, then the album, derived by `computeDoor` from the server's decision `{access, gate}` plus the browser's own facts (18 pins over every permutation; a server gate is terminal for the steps behind it; a stale skip or returning flag cannot open an album when the switch is ON). Require an upload to view is enforced server-side: `resolveGalleryAccess` RETIRED rather than wrapped, so the page, the poll, the guest export and the reel download were each a type error until they learned the gate (live on :3132 the export answered nine photos where it would have zipped fifty-four, the reel 403); `resolveViewerDecision` the one server entry, resolving twice so a locked event or an unconfirmed viewer costs no gate read; `getUploadGate()` on the admin client failing open with a captured warning (both halves verified: uploads closed and a one-byte cap). The server learns which guest is asking through a new HttpOnly cookie `pr_guest_<eventId>` (the raw 64-hex token, Secure, SameSite=Lax, 60 days) written by the mint, the rename, a created media row and the poll's heal (its 304 too), expired by `POST /api/guests/leave` on the guest's sign-out, and read by no write route (pinned by a source test); Privacy moves to 1.4 with the cookie's row. The ETag carries the gate at `g3` (a changed gate never 304s). The queue lifted to the page shell so a run started at the door outlives it; the intent sheet's body and the failure sheet's list exported so the step lives inside the entry sheet; the fail-open server-owned ("Continue without adding" refreshes; a run of unfixable refusals shows the server's sentence); the pre-cookie heal before the arrival beat; a stricter drift waits for the next act. Two findings of its own: an early fail-open on "neither a session nor an account" would have opened the album for every first-time visitor and was removed before it shipped; and the telemetry redaction's 32-hex pattern had NEVER matched a 64-hex session token (no word boundary at character 33), so the longer capability was the one leaking through every Sentry hook, corrected to 32 to 64 hex and pinned. The failure sheet's exit flash from the alias red-team is fixed (its content latches while open). The marketing mock phones and the how-it-works door draw the new shape (mock-parity's browse-out pair deleted rather than retargeted), nine help articles rewritten, guest-flow.md and database-security.md refined. Five declared exceptions outside its owns (a `for` line for the new `upload-step.tsx`, two generated files, the Privacy version constant in `legal.ts`, two depicted counts in the type-ladder policy test for files it owns). Twelve calls his to overrule, one line each in the manifest at `ce3ce0d0` and above all: a contribution punched once; a stricter drift waiting; the cookie carrying the capability itself; the empty album holding the gate; the Back chevron on every step after the first; "Skip for now" and "Continue without adding"; the ON line naming the host; the name before the email in verified mode with the account's name winning; the venue-screen article telling hosts to sign in on the screen; the demo's "Look around" moved to its upload step; the name step's visible label being the question; the browse-out mock pin deleted. Four help articles outside the lane carry staleness for `help-sync` (on the ROADMAP). The first completion's own beat, the R2 PUT localhost cannot make, is the alias red-team's.

- **The door red-teamed on the alias at `27ff8a9e`** (2026-09-21, 22:58 to 23:10 EDT; the pane at 375 signed out on `Gallery width (disposable)` in both switch states, on `Alias red-team (disposable)` given a password and names mode by SQL, on `Ghost check (disposable)` in verified mode, and at `/demo`; Chrome as the host; the poll, the guest export and the reel download by curl): every case of the plan's Verification list holds. Names mode OFF: the welcome held (one Continue, no handle, no close, Escape inert, the album blurred behind), the name step with its Back to the welcome, the upload step with its ghost "Skip for now", the skip opening the album in 261 ms with the name in the header, a second visit straight in, the album's Add opening the intent sheet with a free Escape. Names mode ON: a fresh device meets the welcome over nine blurred tiles and "See all 55", the name, the upload step with no skip and "Will Gibson asked everyone to add a photo before the album opens.", a photo sent from the door landing the full album with the tile marked and the offer card at the album's opening; a reload goes straight in on the cookie; the cookie expired with the storage token kept heals in one poll at 421 ms with no door flash; a guest already inside kept Add and met the step at that act. By curl on the ON event: no cookie or a non-contributor's cookie answers teaser with the upload gate and nine items, a contributor's cookie the full fifty-four, a body token heals with the sixty-day HttpOnly cookie, the ETags differ across gates and the same decision answers 304, the export hands a held guest nine photos against a contributor's fifty-four, the reel answers 403 to a held guest. The fail-open: uploads closed under an open step shows "This event isn't accepting uploads right now." and "Continue without adding" into the full album. The password event: the wrong password refused in place, the right one landing on the name step, the empty album's step reading "Nothing here yet. Add the first photo and the album opens." Verified mode: the name held without a request, then the email step with the account-name line. The demo: its role welcome, the upload step with "Look around", no name step. The host never meets the sheet; the two feature mocks carry no "Just browsing" or "View the album". Two small defects, cut as `door-fixes` (Sonnet, :3135): a STRICTER drift still moves the grid before the guest's next act (the poll's nine tiles replace the fifty-four while the refresh waits), and the 304's cookie heal is set in code but dropped on the wire by Vercel (the code should answer 200 while a heal is pending). One call for Will beyond the lane's twelve: the revisited welcome's primary reads "Back" (it returns you to the step you left; the plan's "Continue"). For his look the disposable events rest as the walk left them: `Gallery width` in names mode with the switch ON (55 media, nine guest rows), `Alias red-team` as a password event in names mode (the password `redteam1`, empty), `Ghost check` verified and empty.

- **`reshape-guest-capture` on the tree** (2026-09-21, merged `7cef61e5` at 23:35 EDT, gate 96 green with 420 smoke checks and the board's 5 demo steps; handed off `3345bb1e` by Sonnet on :3134; lab-only, no production byte): the desk's `guest-capture` tiles audited whole against the shipped door. One violation stood: the `shape` ask's `sheet-step` option still told the pre-ruling story, a guest already in the album dismissing a separate upload sheet with "Not now, take me to the album", against "No exit" and backwards on the facts once that sheet became the held welcome itself; its dismiss now reads "Maybe later", the words the ask wears everywhere else it is drawn. `moment`, `follow`, `landing` and `name` were found accurate and left as drawn. No verdict recorded, no ask added or removed: the board's five questions stand at round 1 for Will's review. Call his to overrule: "Maybe later" on the held sheet rather than a word coined for a sheet with no exit. Deferred to the ROADMAP: the sheet-step peek dims the album behind it with a flat opacity rather than the entry shell's own blur.

- **`door-fixes` on the tree** (merged `11453841` at 00:31 EDT on 2026-09-22, gate 97 green with 419 smoke checks; handed off `46d9353d` by Sonnet on :3135; the door round's last lane): the door red-team's two defects fixed in place. A stricter drift now holds the grid: when a poll's decision is stricter than the one the gallery holds, the narrower payload is not applied and the drift callback fires once, so the album stays whole under a guest the host just gated until their next act (a looser drift applies at once as before). The poll answers 200 instead of 304 while a cookie heal is pending, because Vercel strips `Set-Cookie` from a 304 in transit and the heal the code wrote onto it never reached a browser; a matching validator with the cookie already right still answers 304. Also on the tree from Will's first look (rulings.md "the door's first look"): the revisited welcome's button reads "Continue" and never "Back" (the `continueLabel` prop gone from the welcome step), the ON line reads "The host has asked everyone to add a photo before the album opens." with no host name (the five help mentions are host-voiced paraphrases and stand), and the demo is fresh on every visit (the welcome flag lives in per-mount state for the demo and nothing is ever written; a first cut that pinned the flag false broke the demo's own advance and was caught by the pre-existing demo tests). Calls his to overrule: holding the grid rather than applying the teaser and keeping only the sheet away; the 200 instead of the 304 while a heal is pending; `computeDoor` left untouched because its three flags already read as given facts; the ephemeral state for the demo's welcome over a query-param reset; the dead `continueLabel` prop removed rather than left optional. The re-check on the alias is the Orchestrator's: the drift with the switch flipped by SQL under a guest inside, and the 304 by curl.

- **`door-fixes` re-checked on the alias at `36fcde3c`** (2026-09-22, 00:39 to 00:44 EDT; the pane at 375 signed out on `Gallery width`, the poll by curl, the demo in a second tab): four of five hold. A guest seated in the full album kept all fifty-six tiles and the count line through three polls after the switch turned ON by SQL, with no sheet, and met the door's upload step at their next Add with the line "The host has asked everyone to add a photo before the album opens."; the revisited welcome's button reads "Continue"; the demo shows its role welcome again on a plain reload after Continue and Look around, writing no flag. The fifth does not hold, and the reason is one step upstream of the lane's belief: Vercel's EDGE answers 304 itself whenever the request's `If-None-Match` equals the ETag on the function's 200, dropping `Set-Cookie` in the conversion (a non-matching validator reached the 200 with the cookie; the matching one came back 304 with the ETag echoed and no cookie; the deployment's source commit confirmed through the Vercel API), so answering 200 at the origin changes nothing. Cut as `heal-validator` (Sonnet, :3136): a heal response carries no ETag at all, the one response that always reaches the browser. The lane's five calls stand as listed in its bullet.

- **`heal-validator` on the tree** (merged `9f3d489c` at 03:17 EDT on 2026-09-22, gate 98 green with 420 smoke checks; handed off `69c7c5f4` by Sonnet on :3136 after plan mode paused it with its fix done): a heal response carries no validator. The door-fixes re-check on the alias found Vercel's EDGE, not the function, answering 304 whenever a request's `If-None-Match` equals the ETag on the function's 200, dropping `Set-Cookie` in that conversion; so the poll's pending-heal response now carries no `ETag` at all, the one response that always reaches the browser, and the route's comments tell the true mechanism; a settled cookie with a matching validator still answers 304; the pins rewritten (a pending heal answers 200 with the cookie and no ETag, with or without a matching validator; a settled cookie 304s; a stale validator 200s without a cookie); the poll client already clears its validator when a response has none, so the next poll 200s once and 304s resume; `testing-verification.md` gains the edge's trap. The alias proof is the Orchestrator's curl below.

- **The guest identity round's wave 0 on the tree** (merged `4466802c` at 03:41 EDT on 2026-09-22, gate 99 green with 419 smoke checks; handed off `59c1aa3f` by Opus on :3132; the SQL applied by the Orchestrator): `guests.pending_email` and `pending_email_at` (typed, unproved, never rendered, never mailed on its own, absent from the host's column grant) with their shape check and partial index; `upload_forensics.guest_pending_email`; the token-claimed rows backfilled with `verified_at`; `create_guest` with a fifth `p_pending_email`, nulled beside a confirmed account and on a verified-required event; `set_guest_pending_email` by token, service role only; `list_guest_rows_by_email`, `claim_guest_rows_by_email` and `disown_guest_rows_by_email` for a confirmed caller's own address (the claim stamping `user_id`, `verified_at` and `email`, naming a nameless profile from the most recent claimed row; the disown removing the media through the uploader path and detaching the address); `claim_anonymous_uploads` stamping `verified_at` and `email` for a confirmed caller and dropping a differing typed address; `profile_shown_events` with the public-profile arm on it and on `verified_at`, `profile_hidden_events` kept until a later drop. Applied by the Orchestrator at 03:35 EDT: both files answered success; check 1 rolled back with every guest-identity contract held; check 2's fixture arms could not run on the live project (the only handle belongs to the host of every disposable event, so no attendee on another host's event exists, and a fixture write was refused by the harness) and stand on the lane's throwaway-cluster run of all seven arms with an adversarial role probe; its catalog arms re-ran live and held (RLS on, three owner policies, anon locked out of the table and the four functions, authenticated reaching the claim, pending_email unreadable by authenticated, no capability link in the attended arm; the token in the live payload is a hosted event's own link, by design); the security advisors: the anon set five unchanged, authenticated 34 with exactly the three claim RPCs new, rls_enabled_no_policy 14 unchanged; the types regenerated (68 insertions), typecheck green. The guards and the two arm-pin tests rewritten. Wave 1 (`guest-email-server`, `guest-email-door`, `guest-email-claims`) is cut at this record.

- **The guest identity round's wire on the tree** (merged `e48ce3e1` at 04:35 EDT on 2026-09-22, gate 100 green with 421 smoke checks; handed off `4977e022` by Opus on :3133; the first of wave 1's three): the join accepts an optional `email` on a names-mode event, stores it unconfirmed through `create_guest` and answers `email_attached` (a boolean; the address never comes back; a junk address is a 422 `email_invalid`, "Check that email address."; a blank one is no error); `POST /api/guests/email` attaches, changes or detaches the address by the BODY token behind its own `attach_email` limiter (the rename's numbers; `null` detaches, a missing key is a 400; the cookie is never read as identity); `resolveUploaderIdentity` case 3 returns no address at all, so a typed name's row exposes nothing to the host gallery (its `guests.email` used to print there); `POST /api/guests/capture-email` requires a CONFIRMED session (the crack that could write an unproved address into `guests.email`; zero rows were ever poisoned); the forensics capture writes `guest_pending_email` beside the typed name, read only on the Record export; the profile queries turn to `profile_shown_events` (`getMyAttendedEvents` returns `shownOnProfile`, default off; the covers intersect the shown set), `showEventOnProfileAction` and `hideEventFromProfileAction` replace hide and unhide (the old pair `@deprecated` for one merge, deleted by the claims lane's rewire); `database-security.md` states the two-columns-one-path invariant (`guests.email` confirmed-only, `pending_email` typed-only, a claim the one path). One own exception: `src/lib/errors/codes.test.ts` (the taxonomy's tripwire, nobody's this round, additive). The wire announced to the door and claims lanes.

- **The guest identity round's door on the tree** (merged `d6b65fe9` at 04:54 EDT on 2026-09-22, gate 101 green with 421 smoke checks; handed off `65ba7536` by Opus on :3134; the second of wave 1's three): the name step grew one optional "Email (optional)" field in names mode alone (unfocused, never prefilled, refused in place, sent in the same post as the name; hold and profile modes render none) under the lede "so the host knows who to thank"; the client half of the wire (`joinEvent({email?})`, `emailAttached` read from the row rather than the form, `email_invalid`, `attachGuestEmail`) delegates its parse to the server's `parseGuestEmail`; the device keeps a boolean and never the address; the guest's own menu reads "Unverified" with "Add your email" (a new `add-email-dialog.tsx`, "Confirm it now instead" handing to the code door) or "Email not confirmed" with "Confirm your email" into a code door that opens empty and says why; the public mark's word is "Unverified" everywhere the one constant is read, its two popover sentences naming a confirmed email rather than a verified name; the gate line "The host has asked guests to confirm an email for safety. One tap and you're in." and the email step's description wired verbatim; the offer card prefilled from this visit's memory alone. One defect the brief did not predict, found by the pin: a native email field let the browser block the submit with its own bubble, so the refusal never rendered and the name never went (`noValidate` on both forms). Two help articles rewritten in place; guest-flow.md's three-levels block and auth-accounts.md's inert-address bullet. Five own exceptions accepted (two generated artifacts, one component-notes entry for the new component, two tests that pinned the old label literally and now read the constant). Proven on :3134 at 1440 and 375 against the real database; a real upload under an attached address and the code round trip wait for the alias.

- **The guest identity round's claim ticket on the tree, wave 1 whole** (merged `734eedbb` at 05:12 EDT on 2026-09-22, gate 102 green with 423 smoke checks; handed off `6b6eb9ab` by Sonnet on :3135; the last of wave 1's three): one plain card at the head of the dashboard feed, rendered only while `list_guest_rows_by_email` returns rows, grouped by event with the typed name, the count and the last upload, Claim or Not mine per row, Claim all (sends null, no confirmation) and Finish, which confirms only when something would be left unclaimed ("Permanently delete the N photos and videos added under your email at these M events?", the events named, "Delete and finish" or "Go back"), then `claim_guest_rows_by_email` and, only for a non-empty list, `disown_guest_rows_by_email`; `/welcome` prefills a nameless profile's name from the most recent claimable row when the sign-in carried none; the attended-events switch reads `shownOnProfile` (default off) through `showEventOnProfileAction` and `hideEventFromProfileAction`, the account page's sentence beside it saying events you joined are private until turned on (the two one-merge aliases deleted); Privacy and Terms to 1.5 (the unconfirmed address row, the per-event flag, the claim-or-remove sentence, the opt-in correction); seven help articles current (the five briefed plus the two orphans taken mid-lane); `profiles-social.md` and `host-app.md` refined. Seven own exceptions accepted (the two authorised deletions, the account page's one sentence, the legal version bump, the component notes, two generated artifacts). The card was never driven against seeded rows on localhost (sign-in bounces to production there); the alias red-team drives it with Will's address seeded on two disposable rows. `[preview]`: the alias moves to this record.

- **The guest identity round red-teamed on the alias** (`a9299629` serving from 05:19 EDT on 2026-09-22; held at 05:31 EDT): the name step's optional field (unfocused, never prefilled, the browser's own validation off); a join with a mixed-case address answered `email_attached: true` and never the address, the row storing it trimmed and lowercased, the device holding a boolean and no address, the offer card prefilled from the visit's memory alone; a junk address 422 `email_invalid`; the attach route refusing a missing body token, a cookie-only token and a missing key, detaching on null; the anon role refused the three claim RPCs and the `pending_email` column through PostgREST; the gallery poll and the guest page carrying no address; the public mark "Unverified" on the lightbox credit, the guest list and the host's Guests room, the own popover saying so with "Confirm your email"; the guest menu "Unverified" + "Add your email" flipping to "Email not confirmed" + "Confirm your email" without a refresh, the confirm door opening empty with its sentence; a verified-required door with no field and the gate line "The host has asked guests to confirm an email for safety. One tap and you're in."; THE CLAIM end to end in Will's Chrome with his address seeded on two disposable rows: the dashboard card listing both events with the typed names, counts and dates, Claim one and Not mine the other, Finish opening the confirmation that named the unclaimed event and its count, Go back writing nothing (proven by SQL), Delete and finish stamping the claimed row with his account, `verified_at` and `email` and nulling both pending columns (the guest list and the Guests room reading his profile's name, unmarked), removing the disowned photo through the uploader path (gone from that album's poll; the bin's query excludes uploader removals) and detaching the address, the toast, the card gone; Privacy and Terms at 1.5; nine help articles at their slugs with no old label. Not driven live: Claim all with no confirmation (component-pinned), the token claim from the mark (needs a code; the rolled-back SQL arm stands), and the profile listing, because the only account is the host of every disposable event and hosted events never list as attended (the migration's catalog arm stands). One defect: the confirmation reads "the 1 photo and video" and "these 1 event" for a count of one, a fixes lane. The test rows keep their names and photo as disposable data, their fake addresses nulled.

- **The claims confirmation's sentence fixed** (merged `d05fdbed` at 05:50 EDT on 2026-09-22, gate 103 green with 423 smoke checks; handed off `c5ac7d27` by Sonnet on :3133; the one defect the identity round's red-team found): the confirm-delete title reads naturally for every count, one upload "photo or video" (never the lying singular), several "photos and videos", one event "this event", several "these M events", through one helper, the four forms pinned in the card's test; nothing else in the card changed. `[preview]`: the alias moves to this record.

- **`name-gate` on the tree at `c52f5260`** (2026-09-22, handed off `293c9d9e` by Sonnet on :3133; gate 104 green with 422 smoke checks; Will's ruling of the morning: a nameless account moves nowhere in the app but the welcome page): one `requireNamedProfile()` over the cached profile read, called once from two new layouts under `dashboard/` and `account/`, so the dashboard root, `/new`, every event room and `/account` redirect a nameless account to `/welcome`; a structural test enumerates every top-level `(app)` directory so a future ungated route fails by name and `/welcome` never gates itself; `auth-accounts.md` and `host-app.md` refined (the stale "/account is exempt" line gone). One call his to overrule: the brief's nested `(named)` route group was set aside on the ground (about 45 import sites outside the lane hardcode the old paths), the two layouts covering the same rooms without a move. `[preview]`: the alias moves to this record.

- **`identity-profile` on the desk at `953a249b`** (2026-09-22, handed off `a52f4687` by Sonnet on :3136; gate 105 green with 427 smoke checks; the first of the three identity-flows boards Will sent to the lab): the profile setup and what a page shows on purpose, four asks on Priya's world (`setup`: the account page's cards as shipped, a three-screen wizard, a single sheet from the follow moment; `attended`: the switch list as shipped, a picker of covers, each album offering it from the guest menu; `prompt`: after the first claim, at the follow moment, from the account page only; `page`: a name and "Nothing here yet", the name with the count kept private, not found until something is shown), the shipped state one option per ask, every option drawn, at 1440 and 375; the lane's own tension named in the `page` ask's overrule line (a not-found page would contradict the profiles-are-public-by-existence invariant); registered at the head of the desk and placed by the Orchestrator at the merge. Nothing here wires production; his review after his own project.

- **`identity-claims` on the desk at `0d326d7a`** (2026-09-22, handed off `b692432f` by Sonnet on :3135; gate 106 green with 433 smoke checks; the third of the three identity-flows boards): the claim ticket, five asks on Priya's world after her confirmation, one event really hers and one an impostor's (`ticket`: the plain card as shipped, a banner that opens a sheet, the bell opening a drawer; `pointer`: from the follow moment and the bell; `pass`: the per-event pass; `confirm`: the dialog as shipped, an inline destructive row, a second screen listing every photo about to go; `after`: what the dashboard and the album show once), the shipped state one option per ask, every option drawn, at 1440 and 375; placed after `identity-door` at the merge. Nothing here wires production; his review after his own project.

- **`identity-door` on the desk at `15a35847`** (2026-09-22, handed off `7cf4fc2e` by Sonnet on :3134; gate 107 green with 439 smoke checks; the second of the three identity-flows boards): the door sheet with an optional email, five asks on Priya's world (`field`: the email always open as shipped, a ghost line that opens it, its own step with a Skip; `nudge`: a member's sign-in under the field, a welcome row, none as shipped; `gate`: the verified gate's line as shipped, a list with two benefits, an eyebrow naming the host, kept with its naming tension in the overrule line; `menu`: rows as shipped, a card, a single Your photos row; `remove`: a menu row, a quiet link in the confirm door, nowhere as shipped), the shipped state one option per ask, every option drawn, at 1440 and 375; placed after `guest-capture` at the merge. Nothing here wires production; his review after his own project.

**Next.** The handoffs in the program's order (gate 66 onward), each recorded here through `usher/kit/record.py`; the
identity board's Handoff read against the invariants before its message; the wiring of `guest-verify` after round two
rules; no new board.
