# Light: the twelve treatments, and the four calls left after they are ruled

> **ROLE:** the `light` exploration's standing proposal, kept here so it outlives the track manifest.
> **NOT LAW** until Will rules; the board is `/design/lab/light` while it stands, and every paste it
> offers is generated by the board's own code, never typed by hand. The record of each round is the
> CHANGELOG's entry, and the rulings themselves land in `docs/design/rulings.md`.
>
> **The asks are not here, and neither is the catalog's own copy.** They live in exactly one place, the
> board's own spec (`src/app/(dev)/design/sandbox/light/spec.ts`), which the board, the desk's "Waiting
> on you", the review panel's message and `pnpm lab:review`'s validator all read. A second copy here
> could only go stale, and a ruling recorded against a stale option is worse than no ruling.

## What is ruled

Will, round five (2026-09-16):

- **The kit lands as written.** Light sorts by the JOB it does (separate, fill, mark) rather than by
  where it comes from, and a lamp needs a PLACE (an edge, a boundary, a screen, a plate) rather than an
  object throwing it. That correction is what makes the footer's seam legal under its own doctrine.
- **Phase 1 first.** The shadows land before any chromatic phase: the separate job, whole, on every
  surface, in both modes.
- **Accent is the global register** for the aurora ("identity feels way too weak"), and it may be
  re-tuned later if it ever reads as too strong.

## What is open

**Twelve treatments, ruled one at a time** on the board's catalog (keep, refine or kill, with a note),
in the order Will named them: the light placements, the shadow-specific ones, the marks. Each card is
one specimen drawn twice, as today and with it, with what keeping it lands as platform-wide and up to
two real surfaces already wearing it; the cards of one job share an object, a crop, a ground and a
canvas, so the treatment is the only variable. In full: the seam, the throw, the aurora, the step, the
ring, lift, float, the lit face, the sweep, the bloom, the halo, the beam. Six of the twelve already run
in production unnamed, which is the board's strongest fact and the reason the kill list matters more
than the keep list. Two rulings that used to be separate questions are now just verdicts: keeping both
**lift** and **float** is the shadow family in dark, keeping one is the lift only, killing both holds
today's "Dark: NO shadows anywhere"; and **the lit face** is adopted, refined or dropped on its own
card.

**Four calls that are not one card,** each its own step with its own context and its own specimen: what
lands second (the home page's two ends, paper, or the feature pages), how slowly a lamp breathes (8
seconds or 11), which of the five hues a lamp wears on paper (hand-tuned, one flat correction, or the
dark five unchanged), and the colour of the publish beat (as shipped, the house five, or the five leaned
to violet). Three of the four draw every option on one specimen; the order stays words, because it is a
plan rather than a thing to look at.

**Where the aurora lands** is a fifth question, staged behind keeping the aurora: both of a chapter's
boundaries, one of them, or the whole section as a lit room. It is asked only once that card is kept,
and it is moot the moment it is not. The footer keeps its own seam under every one of them.

## What a wiring round lands

The board's last section generates the blocks, so nothing is typed here: `--shadow-lift` and
`--shadow-layer` with their per-ground alphas, `[data-lgt-cue="lit"]` with its paper half,
`--aurora-cadence` as a sibling of the lamp's clock, the five hues re-declared on `.surface-paper`, the
publish keyframe at 305, one line of the engine (the transform drive's rest state, a law 4 fix), and one
new component, `<SectionLight>`, beside `screen-lamp.tsx`. The fences (what is never done, and the case
behind each one) are folded under the same section.

## What the board still owes Will

A real grain tile, so a chapter-scale field stops banding in 8 bits, and a worst-case pair of
overlapping photographs whose touching edges are both dark, so the lift is judged against the case it
exists for rather than a lucky one. Both are in the board's own asset list and in `docs/ASSETS.md`.
