# Generated / build artifacts — never reformat these
node_modules
.next
out
build
pnpm-lock.yaml
next-env.d.ts

# Generated from the live Supabase schema (keep diffs clean vs. `supabase gen types`)
src/lib/db/types.ts

# shadcn primitives are added/updated by the CLI in upstream style (no semicolons).
# Leaving them un-reformatted keeps `shadcn add` output churn-free and easy to diff
# against upstream. Compose/edit on top of them in your own files, not by reformatting.
src/components/ui

# Markdown is prose. Prettier's prose-wrap reflows it and turns a wrapped line that
# begins with "+"/"-"/"*" into a list item — silently mangling sentences in the docs
# for little benefit. Author markdown by hand; prettier stays on code, where it shines.
# MDX (help/blog articles in content/) is the same: hand-authored prose + first-party
# JSX components; keep prettier off it so inline components + frontmatter stay as written.
*.md
*.mdx

# Vendored third-party source, copied in verbatim under its own licence. The point of
# vendoring is having the exact upstream file: reformatting it destroys the diff against
# upstream and blurs the line between their code and our marked deviations (two in intent,
# seven in-body sites, all tagged PARTYREEL:).
# Compose on top of these in your own files, same rule as the shadcn primitives above.
src/components/vendor

