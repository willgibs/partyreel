{
  "name": "partyreel-backup-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Real-time R2 media-backup Worker (durability-backups.md, Pillar B). Deployed separately from the Next app via wrangler.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dry-run": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^4",
    "wrangler": "^4"
  }
}
