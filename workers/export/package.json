{
  "name": "partyreel-export-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Streaming zip-export Worker for \"Download all\" (uploads-and-r2.md). Verifies a signed manifest token and streams a store-only zip of the named R2 objects. Deployed separately from the Next app via wrangler.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dry-run": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "client-zip": "^2.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^4",
    "wrangler": "^4"
  }
}
