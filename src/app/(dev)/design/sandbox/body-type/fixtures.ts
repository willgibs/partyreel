/**
 * THE STAND-IN CONTENT, round two. `EVENT` is the only survivor of round one's
 * four fixtures (`EVENTS`, `MARKETING`, `RUNS` drew the surfaces round one's
 * OTHER six asks governed: event cards, a feature section, the admin table;
 * none of them are on a button, so `pairs` never reads them and they left with
 * the asks that needed them). Judged for size and wrapping, not for words.
 */
export const EVENT = {
  name: "Mia & Theo's Wedding",
  host: "Mia Calder",
  date: "2026-08-15",
  guests: 34,
  photos: 128,
  description:
    "Everything from the day, in one place. Add whatever you took, whenever you get to it, and we'll keep adding ours. There's no app to install and nothing to sign up for.",
} as const;
