/**
 * THE GENERATOR MOVED HOME (2026-09-20, `avatar-wiring`): `look=diagonal` is
 * ruled and `src/components/ui/avatar.tsx` now imports it for the production
 * path, so the real module lives at `src/lib/avatar/gradient.ts` (its
 * `@contract-for` test moved beside it). This file re-exports it so the board
 * — still open for round two on `look` — keeps drawing with no import changes
 * anywhere else in this directory.
 */
export * from "@/lib/avatar/gradient";
