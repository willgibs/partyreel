Thank you, and one correction so the wrong lesson doesn't travel: two lanes never hit the same file. A test refuses two manifests that own the same path, so the lanes cannot collide except in generated files; that rule is the whole reason six could run. What actually stalled was the machine, not a lock: three merge gates at once (a build, a test run and a headless browser each) pushed the load average past seven, and a cold frame compile on the dev server stopped answering the browser's evaluate call for sixty seconds, which the harness read as a failed step. The fix was one warm retry, not a longer timeout. On heat I can add nothing honest: I never measured it, and the inference isn't local (the agents run in the cloud; the machine runs builds, tests and dev servers), so the resource I was blind to was CPU and disk, not thermal budget. Your general point stands though: the contention that hurts is the one nothing in the manifest names.
